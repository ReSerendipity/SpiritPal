chore(governance): untrack non-core execution docs and duplicate CONTRIBUTING.md

The org-level self-purify exemption for SpiritPal's 6 required-core docs is now live
on ReSerendipity/.github main, so the 6 whitelist files can stay tracked while the
check goes green. This PR removes the *remaining* violations:

- git rm --cached 9 non-core docs under docs/execution/ (8 ondevice research/checklist
  files + 交接指导) - they stay on disk locally, only untracked;
- git rm --cached CONTRIBUTING.md - it duplicates the org-level contribution guide
  already linked from README (Conventional Commits + DCO);
- .gitignore: exact ignore patterns for those files only. No blanket docs/execution/
  rule, because the layout whitelist files (RELEASE_VERIFICATION.md etc.) must stay
  trackable.

After this, self-purify checks 1 (tracked-governance-docs) and 2 (root health files)
both pass; check 3 (README org link) already passes on main.
Signed-off-by: ReSerendipity <zengyangc@outlook.com>
