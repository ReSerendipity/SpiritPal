# 执行对照表 — 桌面分发与安全加固（任务书 2026-09-10）

> 执行方：独立 AI（全自主模式）｜生成日期：2026-09-10
> 对应任务书：`docs/任务书-桌面分发与安全加固-20260910.md`
> 维护规则：阶段 0 起逐项留痕；禁止无记录的"已完成"。

## 一、项目画像（阶段 0.1，2026-09-10 探查确认）

| 维度 | 结论 | 依据 |
|---|---|---|
| 技术栈 | Tauri v2 + React 19 + TS 5.6 + Vite 7 + Tailwind 4 + pnpm；Rust edition 2021（MSRV 1.77.2） | package.json / Cargo.toml / tauri.conf.json |
| 构建命令 | `pnpm build`（tsc -b && vite build）；`pnpm tauri build`（beforeBuildCommand: tsc + vite + obfuscate-and-sri） | package.json scripts / tauri.conf.json build |
| 静态检查 | `pnpm lint`（tsc --noEmit + eslint src/）；`cargo fmt --check`；`cargo clippy -- -D warnings`（CI 1.98.0） | package.json / ci.yml |
| 测试 | `pnpm test` / `pnpm test:coverage`（Vitest）；`cargo test --all-targets`（Rust，tarpaulin ≥21%）；`pnpm test:e2e`（Playwright，CI 部分启用） | package.json / ci.yml / TEST_COMMANDS.md |
| 依赖漏洞扫描 | `pnpm audit` + `cargo audit` + `scripts/security_gate.py`（棘轮基线）+ osv-scanner + trivy（CI） | ci.yml |
| Git 约定 | Conventional Commits：`type(scope): subject`（feat/fix/docs/style/refactor/perf/test/chore/ci/security）；main 直推；DCO 钩子 | git log / docs/agents/SOPS.md §10 |
| 远程 | `https://github.com/ReSerendipity/SpiritPal.git`（gh 已登录，repo+workflow 权限） | git remote -v / gh auth status |
| 版本门禁 | package.json / Cargo.toml / tauri.conf.json 三处一致 + tag v* 匹配 | verify-release-version.yml / SOPS §10.2 |
| 回滚点 | `rollback/pre-task-20260910`（已建）；后续每层级一个 | git tag |

## 二、任务清单（阶段 0.2，AI 拟定即生效）

### P0 层级
| # | 报告建议 | 任务 | 改动文件 | 验收方式 | 状态 | 决策与理由 |
|---|---|---|---|---|---|---|
| P0-1.1 | 报告1 §五/§6/§7 启用增量更新 | 开启 updater：active/createUpdaterArtifacts/dialog；修正 endpoints 仓库名 | src-tauri/tauri.conf.json | `pnpm tauri build` 产出签名更新包（含 .sig） | 已完成 | endpoints 原指向 `spiritpal/spiritpal-app` 与实际远程 `ReSerendipity/SpiritPal` 不符（报告外发现，属 P0 验收必要前提，按实际仓库修正）；JSON 校验通过；完整构建验收并入 P0-1.2 |
| P0-1.2 | 报告1 §6 / 任务书检查清单 #2 | 设置 GitHub Actions Secret `TAURI_SIGNING_PRIVATE_KEY`；核验私钥（已实测验签通过，无密码） | GitHub 仓库级 Secret | gh secret list 非空；本地带 secret 构建签名成功 | 执行中 | 私钥无密码（实测 `--password=` 空密码签名成功）；PASSWORD secret 不设置——GitHub Actions 未设置 secret 解析为空串，与空密码签名一致；构建被 tauri-plugin-notification 版本不一致（Rust 2.3.3 vs npm 2.4.0）阻断 → 已 `cargo update --precise 2.4.0` 对齐（报告外发现 #10），重新构建中 |
| P0-1.3 | 报告2 SOP-17 离线备份 | 私钥离线备份（本机安全目录 + 文档记录），随后删除本机副本 | 备份目录 + 文档 | 备份文件存在且内容与原件一致；src-tauri/keys 私钥已删 | 执行中 | 本机无可访问外部保险库，降级为"用户文档目录备份 + 哈希校验 + 恢复说明"（AI 评定降级，记录原因）；删除本机副本须在 P0-1.2 构建验收通过后执行 |
| P0-1.4 | 报告1 §五-1 / §6 漏更新=永远检查不到 | 创建 updates.json 生成脚本 + release.yml 集成（自动生成并提交 updates.json） | scripts/generate-updates-json.mjs、.github/workflows/release.yml、updates.json（生成物） | 脚本单测；本地生成示例 updates.json 结构正确 | 已完成 | 自动化优先（任务书建议）；与 P1-1 回读验证共用一个 publish job（needs 三平台 release）；脚本 E2E（真实 release 查询）待首次发版验证——当前仓库无 release（gh release list 空），已记录 |
| P0-1.5 | 报告1 §四-7 更新链路 | 前端更新入口接线：托盘"检查更新"菜单、设置页按钮、UpdateNotification 挂载、失败原因可见、版本号动态读取 | src-tauri/src/tray.rs、src-tauri/src/setup/mod.rs、src/components/SettingsWindow.tsx、src/components/UpdateNotification.tsx、src/lib/system/updater.ts、src/App.tsx、src-tauri/capabilities/settings-window.json | `pnpm lint` 通过；`pnpm test` 通过（新增 updater 相关单测）；cargo test/clippy 通过 | 已完成 | UpdateNotification 现存为未挂载死代码（报告外发现）；"每步可见"要求即 P2-2，合并实现；updater 单测 7/7 通过；lint 0 errors；cargo check 通过；capability 补 core:app:allow-version |
| P0-2 | 报告2 §七 Android 弱口令 | 验收现有 fail-closed 实现（commit 00fbe8d 已修复）+ 全仓弱口令复核 + SECURITY_AUDIT 文档同步 | docs/SECURITY_AUDIT_SpiritPal.md | 删除/清空 keystore.properties 后构建无弱口令回退；`git grep` 无 spiritpal123 残留 | 已完成 | 代码现状已 fail-closed（getProperty 无默认值 + GradleException），报告"未见修复标记"源于审计文档未同步（报告外发现）；全仓 grep 无 spiritpal123 残留（仅审计文档历史描述已更新）；**gradle 实测**：keystore.properties 缺失 → 构建成功但不签名（warn 明确提示，无弱口令回退）；缺 storePassword → BUILD FAILED 报"keystore.properties 缺少 storePassword"（fail-closed 生效）；测试文件已清理恢复现场；审计文档已同步修复状态 |

### P1 层级
| # | 报告建议 | 任务 | 改动文件 | 验收方式 | 状态 | 决策与理由 |
|---|---|---|---|---|---|---|
| P1-1 | 报告1 §四-1/§六 发布回读验证 | release.yml 发布后回读验证：资产数量齐全 + 下载哈希与 release 资产清单比对；干净机器验收清单文档 | .github/workflows/release.yml（含 P0-1.4 job）、docs/agents/RELEASE_VERIFICATION.md（新增） | workflow 语法检查；清单文档含安装→启动→更新→卸载无残留步骤 | 已完成（真机项待发布） | 干净机器实机验收无法在 CI 完成，提供清单 + 本机可执行部分（卸载残留检查）作为降级（AI 评定）；CI 自动部分（资产数量 + 签名格式校验）已入 release.yml publish job |
| P1-2 | 报告1 §四-10 SmartScreen 引导 | README + 发布说明增加"无签名 SmartScreen 提示属正常"引导话术；不得伪造证书 | README.md、.github/workflows/release.yml（releaseBody） | README 段落存在；releaseBody 含引导话术 | 已完成 | 直接按任务书话术模板落地 |

### P2 层级
| # | 报告建议 | 任务 | 改动文件 | 验收方式 | 状态 | 决策与理由 |
|---|---|---|---|---|---|---|
| P2-1 | 报告2 §二 capability 最小授权 lint | 新建 scripts/lint-capabilities.mjs：拒绝 sql:allow-execute/shell:allow-execute/*://*；校验 invoke 命令与注册命令/权限消费对应；接入 structure-guard + pre-commit；负向测试 | scripts/lint-capabilities.mjs、.github/workflows/structure-guard.yml、.pre-commit-config.yaml、scripts/__tests__/lint-capabilities.test.mjs | 负向测试（注入 sql:allow-execute 必失败）；现有 3 个 capability 通过 | 已完成 | "未消费即失效"方向改为"capability 前缀对应的插件/API 有真实实现"粗校验 + "invoke 命令必须已在 Rust 注册"反向校验（permission 标识符与 invoke 命令名无直接一一映射，AI 评定降级并记录）；负向测试 6/6 通过；已接入 CI/pre-commit |
| P2-2 | 报告1 §四-7 每步可见 | 并入 P0-1.5；状态机 idle→checking→downloading(进度)→verifying→installing→restarting，失败显示原因 | 同 P0-1.5 | 见 P0-1.5 | 已完成 | 合并执行，验收项独立（updater 单测覆盖阶段回调顺序与错误透传） |
| 阶段五-1 | 报告1 §阶段2 必需能力：崩溃自启 | 后端崩溃自动重启（现有 diagnostics panic hook 基础上追加受限重启：计数+冷却防循环） | src-tauri/src/diagnostics.rs、src-tauri/src/lib.rs | cargo test 新增单测；cargo clippy 通过 | 已完成 | 托盘/单实例已实现（lib.rs/setup 确认）；崩溃自启未实现 → 按任务书"若无则补"，最小实现无新依赖（AI 评定）；单测 5 项新增全过；支持 SPIRITPAL_DISABLE_CRASH_RESTART 关闭 |
| 阶段五-2 | 任务书阶段五 文档与门禁收口 | GOTCHAS（KNOWN_GOTCHAS.md）追加本任务坑；SOPS（AI_DEV_SOPS.md）追加 SOP-增量更新发布链；托盘/单实例/崩溃自启文档佐证 | docs/project/KNOWN_GOTCHAS.md、docs/project/AI_DEV_SOPS.md、README.md、docs/SECURITY_AUDIT_SpiritPal.md | 文档条目存在且格式与既有条目一致 | 已完成 | 文档实际存放于 docs/project/（报告外发现：任务书所述 docs/agents/GOTCHAS.md 为索引转发）；GOTCHAS #64-66、SOP-5、README 桌面能力小节、SECURITY_AUDIT 修复状态同步 |

## 三、报告外发现（仅记录，除标注外不擅自处理）

| # | 发现 | 与报告的关系 | 处理 |
|---|---|---|---|
| 1 | tauri.conf.json updater endpoints 仓库名 `spiritpal/spiritpal-app` 与实际远程 `ReSerendipity/SpiritPal` 不符 | 报告 §1 记录现状但未识别错误；不修正则 P0 验收（实测可检查到新版本）必然失败 | 随 P0-1.1 修正（属 P0 任务必要前提） |
| 2 | GitHub Actions secrets 为空（TAURI_SIGNING_PRIVATE_KEY 未设置） | 任务书 §1 称"确认存在"，实际不存在 | 随 P0-1.2 设置 |
| 3 | 私钥 petpal-updater.key 无密码 | 任务书称"私钥（含密码）" | 随 P0-1.2 决策：PASSWORD secret 按空处理 |
| 4 | Android build.gradle.kts 弱口令已修复（commit 00fbe8d），SECURITY_AUDIT 未同步 | 报告"未见修复标记" | 随 P0-2 文档同步 |
| 5 | UpdateNotification 组件未挂载（死代码），前端更新链路未接线 | 报告 §1 未识别 | 随 P0-1.5 接线 |
| 6 | 关于页版本号硬编码 "0.1.0" | 任务书避坑指南 #1 已点名"版本号显示旧值" | 随 P0-1.5 改为动态读取 |
| 7 | GOTCHAS/SOPS 完整内容在 docs/project/（docs/agents/ 为索引） | 任务书阶段五指向 docs/agents/ | 随阶段五-2 追加到实际文件 |
| 8 | capabilities 已含 updater:allow-check/download-and-install 授权 | 任务书未提（无需改动，仅记录） | 无需处理 |
| 9 | src-tauri/keys 目录仅 .key/.key.pub 且均被 .gitignore 忽略 | 报告 §1 确认 | 无需处理（私钥删除后 .pub 保留亦可） |
