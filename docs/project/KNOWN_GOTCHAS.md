<!-- 注记：本节内容于 2026-08-27（家族规范治理 Phase E3）自 AGENTS.md 整节逐字移出，一个字符未改；此后新坑按铁律 #2 直接追加到本文件表格末尾 -->
# 常见陷阱（Known Gotchas）— 血泪教训汇总（自 AGENTS.md 移出）

## 14. 常见陷阱（Known Gotchas）— 血泪教训汇总（原缺失，现在开始累积）

<!-- 📥 新坑追加模板（AI 踩坑后复制填好追加到表格最后）：
| # | 坑点标题 | 触发场景 | 现象/报错 | 正确做法 | 首次发现日期 |
|---|---------|---------|---------|---------|------------|
| X | 简短标题 | 什么操作会触发 | 具体报错信息或现象 | 正确代码/配置/步骤 | YYYY-MM-DD |
-->

| # | 坑点标题 | 触发场景 | 现象/报错 | 正确做法 | 首次发现日期 |
|---|---------|---------|---------|---------|------------|
| 1 | **Rust 改动后只 pnpm dev 没重新 build → E2E 跑的还是旧 Rust 代码** | 改了 `commands/pet.rs`，想快速验证 → 直接 `pnpm dev` 然后 Playwright E2E | E2E 结果和预期不符、调试半天发现 Rust 端新命令根本没进 E2E 二进制（因为 dev 模式下 Tauri 可能缓存旧的 plugin） | **改完 Rust 代码必执行 pnpm build 一次**（见 §7.1 Build After Code Changes），然后复制到 artifacts/ → E2E 指向 artifacts 下的二进制跑 | 2026-08-10 |
| 2 | **3 处版本号（package.json + Cargo.toml + tauri.conf.json）必须一致** | 只改 package.json 的 `version`，忘记 Rust 端两个文件 | `pnpm build` 成功，但 CI `check-version-consistency.js` 报错：`version mismatch: pkg=1.0.1 cargo=1.0.0 tauri=1.0.0` → PR 失败 | 改版本直接按 §10.2 同步改 3 个文件，或者先让 release-please 自动改（如果项目接入了），改完本地跑 `node scripts/check-version-consistency.js` 先过一遍 | 2026-08-10 |
| 3 | **Tauri command 必须加入 generate_handler! 数组** | 写完 `cmd_pet_hug` 函数，忘记加到 `tauri::generate_handler![cmd_pet_get, cmd_pet_feed, ...]` 里 | 前端 invoke 报错 "command spiritpal:pet_hug not found"，搜 Rust 代码里函数确实存在，调试 1-2 小时才发现忘记加到宏数组 | 每次新增 command 的最后一步 **强制 checklist：「函数已加入 generate_handler! 数组 ✓」**（见 SOP-1 步骤 3） | 2026-08-10 |
| 4 | **Tauri 2.x Capability scope 必须显式写** | 写了个 fs.readFile 读 app_data_dir 的图片，capability allowlist 只写 `"fs": { "readFile": true }` 没 scope | 运行时报 `PermissionDenied: not in scope: $APPDATA/images/avatar.png`，生产环境图裂 | 每个 capability 的 fs 权限都显式写 scope：`"fs": { "readFile": true, "scope": ["$APPDATA/**", "$ASSET/**"] }`，不写 scope 默认空集 = 全拒绝 | 2026-08-10 |
| 5 | **Live2D 模型路径大小写敏感** | macOS 开发一切正常（`Miko.model3.json`），Windows 部署后模型加载失败控制台 404 | Windows NTFS 默认大小写不敏感所以开发没问题，但 Tauri asset 协议是 case-sensitive 的，生产构建 404 | 模型所有引用路径（代码里 `modelPath` + `.model3.json` 内部的 texture 路径）**全小写 + 下划线命名**（`miko_001.model3.json`） | 2026-08-10 |
| 6 | **Zustand persist 版本迁移** | settingsStore 新增了字段 `new_feature_flag: boolean`，但用户设备上存的是老版本 JSON（没有这个字段） | 应用启动报错：`Cannot destructure property 'new_feature_flag' of '...' as it is undefined`，新用户正常老用户全炸 | persist 必须配 `version: 1` + `migrate: (persistedState, version) => {...}`，迁移老版本 JSON 自动补默认值。每改一次 store schema version +1，写对应 migrate 逻辑 | 2026-08-10 |
| 7 | **Tailwind 颜色语义 Token 别写硬编码** | 新组件写 `className="bg-blue-500 text-white"`，过了 1 周产品说「我们要换紫色主题」 | grep 整个项目搜 blue-500，100+ 处硬编码，改到想死 + 必然漏改导致颜色不一致 | **所有 UI 组件颜色只能用语义化 Token**：`bg-pet-primary` / `text-pet-on-surface`（在 tailwind.config.js → `theme.extend.colors.pet` 里定义），换主题改一处全应用生效 | 2026-08-10 |
| 8 | **窗口拖拽 + 点击穿透互斥** | pet-window 启用「点击穿透（transparent mouse）」然后想同时支持「拖动宠物」 | Windows 上开启 WS_EX_TRANSPARENT 后窗口收不到任何鼠标消息 → 拖不动宠物，卡死 1 小时 | 两种模式互斥：拖动时**临时关闭穿透**（`lib/windowManager.setClickThrough(false)`），拖完松手再开回去。加 hook `useDraggableWindow` 内部处理切换。 | 2026-08-10 |
| 9 | **`#![forbid(unsafe_code)]` 下不允许写 FFI unsafe** | 想调一个 Windows API `SetWindowPos` 改窗口位置，手搓 `extern "system" { fn SetWindowPos(...) }` + `unsafe { SetWindowPos(...) }` | `cargo build` 直接报错：`forbidden unsafe_code attribute`，构建失败。想改成 `#![allow(unsafe_code)]` 又过不了 PR review | 用 Tauri 官方 `tauri-plugin-window` 已经封装好的 `window.set_position(x,y)` 安全接口。绝对不要自己写 unsafe，哪怕你认为你能证明安全。 | 2026-08-10 |
| 10 | **加密算法参数固定后绝对不要改** | 上线 2 个月后想：「Argon2 t=3 是不是太慢？改成 t=1 吧」 | 用户升级后打开 App：所有历史聊天记录、宠物档案 **全部 AES-GCM 解密失败**（因为 Argon2 派生出的密钥变了）→ 用户数据丢失，灾难性事故 | 上线前定好算法参数（AES-256-GCM IV=12B / Tag=16B，Argon2id m=65536 t=3 p=1），写死为 `pub const ENCRYPTION_PARAMS` 常量。想升级只能做「双算法兼容 + 后台迁移」，不能直接改老数据的算法。 | 2026-08-10 |
| 11 | **功能性改动后必须编译桌面端和移动端才能看到结果** | 修改了角色配置、UI 组件、业务逻辑等前端/共享代码后，只运行 `pnpm dev` 或 `pnpm test` 就以为改动生效 | 开发模式下改动看似生效（Vite HMR），但生产构建或实际应用中改动未体现；移动端完全未更新 | **任何功能性改动完成后，必须执行完整编译流程**：① 桌面端：`pnpm tauri build`（生成 Windows/macOS/Linux 安装包）；② 移动端：`pnpm tauri android build --apk` 或 `pnpm tauri ios build`（生成移动安装包）。编译产物复制到 `artifacts/` 目录进行验证。仅运行测试或开发模式不能替代完整编译。 | 2026-08-12 |
| 12 | **响应式触发限流不能有全局间隔检查** | T-8 给响应式触发加了 `canTriggerResponsive()` 含 15min 间隔检查 | 测试失败：周期触发 `recordTrigger('frequency')` 后，响应式触发也被全局间隔拦截 → 用户主动对话时无法触发情感回忆 | 响应式触发是用户驱动的（非主动打扰），只需检查每日总上限，**不加全局间隔检查**。间隔检查仅适用于主动触发（`canTrigger`） | 2026-08-15 |
| 13 | **Rust `computeContextFit` 中不能用 `await import`** | T-9 给 `computeContextFit` 加音乐信号时用了 `await import('./musicAwareness')` | `tsc` 编译通过但运行时报错：`computeContextFit` 是同步方法，不能使用 `await` | 同步方法中引用外部模块必须用顶部静态 `import`，不能用动态 `await import` | 2026-08-15 |
| 14 | **无边框窗口的窗口操作（最小化/最大化/缩放）必须显式加 capability 权限，否则静默失败** | settings/chat 窗口是 `decorations: false`，`WindowControls` 调 `minimize()/toggleMaximize()`、`FramelessResizeHandles` 调 `startResizeDragging()`，但 `capabilities/*.json` 里没写对应 allow | 按钮点了没反应、鼠标拖边缘无法缩放，**控制台无任何报错**（Promise 被 `.catch(() => {})` 吞掉），排查半天 | `core:window:default` 只含只读权限，写操作必须显式加：`allow-minimize` / `allow-maximize` / `allow-unmaximize` / `allow-toggle-maximize` / `allow-start-resize-dragging`；同时 `WebviewWindowBuilder`/前端 `WebviewWindow` 必须 `resizable: true`（`resizable: false` 的窗口在 Windows 上即使有权限也无法最大化） | 2026-08-19 |
| 15 | **面板/浮层可拖动时，顶层拖拽条（data-tauri-drag-region）必须让出 z-index** | pet-window 顶部有一条 h-8 的 `data-tauri-drag-region` 拖拽条（z-50），状态面板默认 `right-1 top-1` 恰好被它盖住 | 按住面板顶部（名字行）拖动 → 触发的是整窗拖拽而不是面板拖动，面板永远拖不走 | 拖拽条 z-index 降到面板（z-30）之下；面板拖拽用「3px 阈值后才 setPointerCapture」模式（立即 capture 会吞掉面板内按钮的 onClick）；拖拽期间临时关闭像素穿透（`usePixelClickThrough(!panelDragging, ...)`）防 WS_EX_TRANSPARENT 截断事件 | 2026-08-19 |
| 16 | **桌宠窗口内滚动型浮层（右键菜单等）滚不动 = max-h 超窗口 + 根节点 wheel 拦截 + 穿透白名单缺失三重叠加** | pet-window（高 400）右键弹出菜单，菜单 `max-h-[420px]`；滚轮滚动菜单 → 宠物被缩放、菜单不动；菜单底部项（退出）被窗口裁掉一半 | 菜单永远滑不到底；截图可见最后一项只有上半部分 | ① 浮层 max-height 用 `Math.min(420, winH - 8)` 动态计算，保证 ≤ 窗口视口高（否则容器底部被窗口物理裁剪）；② 浮层容器必须 `onWheel={e => e.stopPropagation()}`，否则滚轮冒泡到 PetWindow 根节点的 `handleWheel`（缩放宠物，preventDefault 吞掉原生滚动）；③ 浮层容器/遮罩必须加入像素穿透交互白名单（`[data-spiritpal-menu]` / `.spiritpal-menu-overlay`），否则悬停浮层空白区时窗口被切到 WS_EX_TRANSPARENT，滚轮事件直接丢失 | 2026-08-19 |
| 17 | **窗口边缘吸附判定不能用「窗口中心点」，必须用「窗口边缘距屏幕边」** | v1.7 实现实时磁吸时用中心点判定 + `max(40, 窗口宽×20%)` 阈值 | 默认 300px 窗口半宽 150 > 阈值 60，放大到 600px 后半宽 300 > 阈值 120——窗口中心永远到不了阈值范围，**吸附整体失效**（小窗口也不吸） | 判定改为窗口边缘距屏幕边缘 < 阈值（`x - screenX < thresh` / `screenX+screenW-(x+winW) < thresh`），窗口贴边时边缘距边为 0 恒满足；中心点只用于防卡屏（鼠标距中心超窗口尺寸时跳过吸附） | 2026-08-19 |
| 18 | **宠物头顶气泡（bottom-full 定位）在窗口高度只按精灵尺寸计算时被顶部裁剪** | 滚轮把宠物放大到 3×（精灵 624 高），handleWheel 的 needH = spriteH+32，宠物顶部只剩 24px | 气泡显示在宠物容器正上方，被窗口顶部物理裁剪，文字/图形显示不全 | 窗口目标高度预留气泡空间：`needH = max(WIN_H, spriteH + 32 + BUBBLE_TOP_SPACE(64))`，宠物 pos.y 相应下移；小窗口（默认 400）不受影响 | 2026-08-19 |
| 19 | **Tauri v2 的 `currentMonitor()` 是顶层函数，Window 实例没有该方法** | usePetDragging 写 `(win as any).currentMonitor()` 获取当前显示器 | 拖动开始缓存屏幕环境时抛 `TypeError: win.currentMonitor is not a function` → catch 吞掉 → dragEnvRef 恒为 null → **边缘吸附永不生效**（实时磁吸+释放吸附全部失效）；单测 mock 了 currentMonitor 掩盖此 bug，真实环境必现 | 用顶层函数：`import { currentMonitor } from '@tauri-apps/api/window'` 后直接 `currentMonitor()`；写 Tauri API 调用前先确认是模块函数还是实例方法（window.js 导出表）；排此类"功能不生效"先查运行时 console（CDP 注入日志最快） | 2026-08-19 |
| 20 | **气泡/浮层驱动的窗口自适应不能依赖「未贴边」状态——贴边停靠是常态，守卫会静默跳过导致长文本照旧被裁剪** | v2.7 气泡窗口自适应加 `if (dockDirRef.current) return` 守卫（担心贴边时改窗口尺寸和吸附抢位置） | 用户反馈长段说话仍被窗口顶部裁剪：宠物被拖到屏幕边缘（或启动时窗口移动 400ms 后 onMoved 自动 snapToEdge）→ dockDir 非空 → 气泡放大被静默跳过；底部停靠的宠物视觉上"正常站立"，极难察觉已贴边 | ① 自适应逻辑不要用 dockDir 做守卫（贴边时照常放大/恢复，改尺寸不解除吸附）；② applyWindowSize 锚定改为贴边感知：dir=left/right/top/bottom 时固定对应屏幕边缘（newX = dir==='left'? pos.x : dir==='right'? pos.x+dW : pos.x+round(dW/2)；newY = dir==='top'? pos.y : pos.y+dH），未贴边时保持中心 X/底部 Y——底部贴边时窗口向上生长、宠物原地不动；③ 验证手法：PowerShell GetWindowRect 每 2s 采样窗口矩形，观察气泡出现时高度增长、关闭后恢复，底边锚定恒定 | 2026-08-22 |
| 21 | **eslint react-hooks v7 的 purity 规则对「事件处理器内 Date.now/Math.random」的判定随组件结构变化而漂移** | v2.8 大改 PetWindow（新增面板/处理器/渲染分支）后 lint 从 0 报错变成 8 个 purity 错误：onDragStart/onDragEnd/pickBubble/triggerPet/handleFeed/handlePlay 等多年未动的代码全被标「Cannot call impure function during render」；且同一批 handler 中 handleBathe 没被标、handleStartPomodoro 被标，判定不一致 | 反复出现「改动前 eslint 全过、改动后老代码报错」且报错位置与改动无关，无法从代码语义推断规律 | ① 先修 React Compiler 的 memoization 报错（如 showBubble 依赖补 `setBubble`，稳定 setter 加进 deps 无害），编译器恢复完整分析后事件处理器通常不再被误伤；② 剩余报错按行逐个补 `// eslint-disable-next-line react-hooks/purity -- 仅事件处理器执行路径（xxx），非渲染路径`（与既有 onClick 注释同模式），不要批量加；③ 加完跑 lint 把 unused directive 的删掉（编译器判定漂移会导致部分豁免多余）；④ 判定基准 = 当前 eslint 实跑结果，不是代码语义 | 2026-08-22 |
| 22 | **宠物行走目标范围硬编码窗口尺寸，窗口/布局变化后宠物游走越界（"被挤到边缘"的假象）** | v2.10 面板并排布局后，用户反馈「打开左侧状态卡后宠物被挤到边缘」 | 根因：usePetBehavior 行走目标 `minX=8 / maxX=WIN_W(300)-SPRITE_W-8` 写死旧窗口宽；面板展开后窗口 444 宽、宠物本应在 x=188 列间区，但行走仍把它带向 x∈[8,100]——正好走到左侧状态卡后面，视觉上"被挤到边缘" | 行走范围改为由调用方注入 `getWalkBounds`（面板展开时返回列间缝隙区间，区间 <24px 则跳过行走改 idle；收起态返回 null 用默认范围）；任何改变宠物活动区域的布局（面板、窗口自适应）都要同步约束行走范围，宠物位置（pos.x）与行走目标必须同一坐标系 | 2026-08-22 |
| 23 | **组件内「渲染期读 ref.current」与「useCallback 闭包引用的 setter 漏进 deps」会被 eslint react-hooks 双规则拦截** | v2.14 重写 RoamWindow 时：① displayState 计算里读 `pausedRef.current` 判断行走态；② handlePanelInteractiveChange 的 useCallback deps 写 `[]`（自认为 ref+setter 稳定无需依赖） | ① `Cannot access refs during render`（react-hooks/refs，error）——渲染期读 ref 不触发重渲染，行走状态会显示错乱；② `Existing memoization could not be preserved`（react-hooks/preserve-manual-memoization，error）——React Compiler 推断依赖 `setPanelOpen` 与手写 deps 不一致直接跳过优化 | ① 渲染期判定用 state 镜像：pauseWalk/resumeWalk 同步 `setWalking`，displayState 只读 `walking && !panelOpen`，ref 仅给 interval/事件回调做即时判定；② 稳定 setter（如 `setPanelOpen`）直接加进 useCallback deps 无害（Gotcha 21 同款经验），编译器能保留 memoization | 2026-08-24 |
| 24 | **前端 WebviewWindow 创建的新窗口必须加入 capabilities 配置，否则该窗口所有 core IPC 被拒（自定义 command 不受限）** | v2.14 漫游窗口交互链路排查：roam-window 由 pet-window 通过 `new WebviewWindow('roam-window', ...)` 创建，但 capabilities 只有 default(pet-window)/chat-window/settings-window 三个文件，无任何文件覆盖 roam-window | 漫游窗口内 `getAllWindows`/`hide`/`show`/`setFocus`/事件监听全部 Promise reject（前端 catch 吞掉无提示）→ 「退出漫游/打开聊天/设置」全部无反应（用户反馈"连退出都无法操作"）；但像素穿透（get_mouse_pos/set_pet_click_through 等自定义 command）正常 → 宠物能走动，观感像"纯动画" | Tauri v2 capability 按窗口 label 匹配，窗口不在任何 capability 的 windows 数组里则该窗口 core IPC 全拒；**新建窗口（含前端动态创建的）必须同步创建/并入 capability 文件**（权限最小化：只加该窗口实际用到的 core:window:* / core:event:* / monitor 查询，勿带 store/sql/notification）；排查"窗口内功能集体不生效"先核对 capabilities 是否覆盖该窗口 label | 2026-08-24 |
| 25 | **WebView2 `additional_browser_args('--remote-debugging-port=...')` 注入 CDP 后，同 userData 下前端新创建的窗口（参数不同）创建失败** | v2.14 想用 CDP 做漫游 DOM 验证：给 pet-window（Rust WebviewWindowBuilder）加 `--remote-debugging-port=9222`，进入漫游时前端 `new WebviewWindow('roam-window')`（无额外参数） | 点击「漫游」→ pet-window 隐藏成功、roam-window **创建失败**（窗口枚举无漫游窗口）→ 应用卡在"pet 隐藏 + roam 未建"的中间态；`WEBVIEW2_ADDITIONAL_BROWSER_ARGUMENTS` 环境变量也被 WebView2 内部参数覆盖无效；**roam-window 创建成功与否与 CDP 参数存在直接因果**（还原参数后同操作即成功） | WebView2 中同 userData 目录的多个 webview 必须用**兼容的 browser args**，参数不一致时新 webview 创建失败；前端 `WebviewWindow` options **不支持 additionalBrowserArgs**（TS 报错），env 变量不生效 → **CDP 调试与"前端动态创建窗口"互斥**；需要 DOM 级验证时改用临时自动钩子（如漫游窗口挂载 2s 后自动调用目标 IPC，从 OS 窗口状态变化推断权限生效），验证完移除 | 2026-08-24 |
| 26 | **vitest `vi.mock` 工厂引用测试文件顶层 const 会因 mock 提升（hoisting）进入 TDZ 而静默失效——mock 的模块被真实实现替代** | v2.15 写 petForm.test.ts：`const petWin = {...}` + `vi.mock('@tauri-apps/api/window', () => ({ getAllWindows: () => Promise.resolve([petWin]) }))` 引用外层 petWin | 测试"3 failed | 1 passed"且失败诡异：mock 的 updateSettings 0 次调用、petSize 设置不生效（实际用了真实 settingsStore 的 1.0）、断言收到真实 PhysicalPosition/PhysicalSize——因为 vi.mock 工厂被提升到文件顶部执行，此时 petWin/mockSettingsState 处于 TDZ（undefined），mock 工厂静默产出 undefined → 模块回落真实实现；且路径写错（`../stores/settingsStore` 相对测试文件解析不到 src/stores）也会导致 mock 不匹配 | ① 测试用的 mock 数据（含 vi.fn()）必须放 `vi.hoisted(() => ({...}))` 中，mock 工厂引用 hoisted 返回值；② vi.mock 路径相对**测试文件**解析（`__tests__` 在 src/lib/ 下 → `../../stores/settingsStore`）；③ 出现"断言收到真实类实例（如 PhysicalSize）"= mock 未生效的第一信号，先查 hoisting 与路径 | 2026-08-25 |
| 27 | **WebView2「canvas 能读到 VP9 alpha 但显示合成丢弃 alpha」——色度键检测用 hasAlphaChannel 短路必然误判，黑底原样显示** | 视频宠物（咕咕嘎嘎 webm）Windows 上按住/静止都黑底；chromaKey 检测逻辑 `hasAlphaChannel→return false`（假设"canvas 有 alpha = 显示正常"） | 运行时实测（debug+CDP 逐像素）：canvas getImageData 透明区 alpha≈14、身体 255（**alpha 平面确实被解码**），但显示合成阶段丢弃 alpha → 用户看到黑底；检测因此返回 false → chromaKeyMode 恒 false → 修复前黑底永远在（单测也测不出：纯逻辑测试 mock 不到 WebView2 的解码/显示分离） | ① 检测**去掉 hasAlpha 短路**，仅按显示层可观察信号判定：四角近黑占比 ≥30% → 启用色度键；② 抠像双策略（drawChromaKeyFrame 逐帧选）：帧自带 alpha → `applyAlphaKey`（alpha<192 置透明，背景≈14 被抠、**不透明黑色身体保留不受损**）；无 alpha（纯黑幕不透明素材）→ `applyChromaKey`（RGB≤12，OC-Claw 原案）；③ 调试通道注意：document.title 不同步到 frameless 窗口 HWND 标题（GetWindowText 读不到）、webview console.log 不进 tauri-plugin-log 的日志文件 → DOM 观测必须走 debug 构建 `additional_browser_args("--remote-debugging-port=9222")` + playwright connectOverCDP（或临时 hook）；④ 副作用不进 setState updater：检测（DOM 操作）移到 setState 外先算结果 | 2026-08-26 |
| 28 | **视觉感知存在两套实现：孤岛 `visionPerception.ts`（getVisionPerceptionManager/petLookAround）与已接线的 `visualPerception.ts`（getVisualPerceptionManager/triggerAnalysis）；`take_screenshot` 是「计划中尚未实现」命令** | 接《剩余任务交接报告》A-1「看看」功能，按报告点名去接线 `visionPerception.ts` 的 `petLookAround` | 实测：`visionPerception.ts` 全仓除自身与 `promptRegistry`/测试外无运行链路 importer（真·孤岛）；`petLookAround` 内 `screenshotFull()` 调 `invoke('take_screenshot')` 因命令未实现必抛 → 整条返回 null；而 `take_screenshot`/`analyze_screen_content` 在 `ipcContract.test.ts:49` 被显式列为「计划中尚未实现」命令。若照报告接 `visionPerception`，「看看」永远走 onError 兜底、永无真实描述 | ① A-1 面板「看看」应接**已接线的** `visualPerception.ts` 的 `getVisualPerceptionManager().triggerAnalysis()`（ChatWindow 同款生产路径），其 `captureScreen()` 在 `take_screenshot` 缺失时优雅返回 null、`get_active_window`（已存在）仍可用；② 真实「屏幕描述」需 `take_screenshot` Rust 命令落地或提供 `onLLMAnalyze` 回调，否则只走「活动窗口推断」分支（仍可产出"当前窗口: X (Y)"）；③ 不要为追求"真实描述"而假实现截屏；缺失时按报告 A-1 兜底要求展示引导文案即可 | 2026-08-27 |
| 29 | **`silentModeManager` 的 meeting/focus 自动静音与 F9 全局快捷键是 TODO 空实现** | 接《剩余任务交接报告》A-5 静默模式，期望"会议/专注状态自动静音" | `silentModeManager.ts` 的 `enableAutoMeetingSilence()`/`enableAutoFocusSilence()`/`setupGlobalShortcut()`/`setupSystemEventListeners()` 均为空壳（仅 `console.log`），不订阅 `contextAwareness` 也不注册快捷键。A-5 仅接好了：手动切换（右键菜单"静默/取消静默"）、23:00–7:00 计划静音、以及对 `proactiveSpeak` 自发开口 + `ChatWindow` 助手回复 TTS 的静默拦截 | ① 若需会议/专注自动静音，需在 `setupSystemEventListeners` 内订阅 `getContextAwarenessManager().onStateChange` 并在 `workState==='meeting'/'focus'` 时调用 `muteForReason(...)`（reason 机制已就绪，仅缺订阅接线）；② F9 快捷键用 Tauri globalShortcut 插件注册；③ 未实现前不要把"会议自动静音"当作已交付功能写进发布说明 | 2026-08-27 |
| 28 | **Windows 退出时加密落盘失败：SQLite 连接关闭后文件锁延迟释放 + 启动双解密竞争** | V-1 验收：安装包对话后正常退出，`%APPDATA%` 下明文 `spiritpal.db` 残留、无 `.enc`。根因①：beforeunload 直接 `invoke('encrypt_db_at_rest')` 时前端 DB 连接未关闭 → Rust `fs::remove_file` 被锁失败；②：启动时 Rust spawn 异步解密与前端 initDB `invoke('decrypt_db_at_rest')` **并发执行**（两个解密同时写 spiritpal.db） | 明文 DB 不落盘即安全缺口（记忆/聊天数据全裸）；退出时静默失败（Rust 侧 Err 被忽略）；排查发现启动解密实际被调用两次 | ① 加密前必须先关闭 SQLite 连接：前端 `encryptDatabaseAtRest()` 先 `Database.close()`（WAL checkpoint）再 invoke 加密；② Rust `RunEvent::ExitRequested` 加 **300ms 延迟**等前端 beforeunload 完成，加密/删明文均带重试（PermissionDenied → 100ms×5，`remove_file_with_retry`）；③ **加密失败保留明文不删除**（宁可明文不删也不丢数据）；④ 启动解密统一由前端 initDB 负责，移除 Rust spawn（消除双解密竞争）；⑤ 验收手法：安装包对话→正常退出→检查 `.enc` 生成且明文/-wal/-shm 无残留 | 2026-08-26 |
| 29 | **3 套角色包清单格式互不兼容 + Rust 命令未注册 → 导入器看起来有代码但永远空结果** | characterResourceImporter 调用 `scan_character_directory`/`read_text_file` 但 lib.rs invoke_handler 未注册 → `invoke` 静默 catch 返回 `[]`（importer L277-281）；同时 CharacterPackConfig / CharacterResourcePackage / PetMetadata 三套格式字段各异，任何导入器接 UI 都各说各话 | 路径 E（资源包导入）看起来完整实际永远空结果；SillyTavern 卡导入只返回 Partial 不持久化 → 重启后消失 | ① 新建 `lib/characterPack.ts` 统一解析中枢（parseCharacterPack 识别 3 种格式 → CharacterProfile）；② Rust 新建 `character_import.rs`（scan_character_directory + read_text_file，含路径穿越防护 + 1MB 限制）并注册到 generate_handler!；③ 新建 `lib/characterImportService.ts` 统一入口 `importCharacter(source)` 汇聚 JSON/PNG/目录导入 → parseCharacterPack → 持久化；④ 视频类型自动 chromaKey='auto'（与内置 gugugaga 一致） | 2026-08-26 |
| 30 | **`dailyJournal`/`calendarIntegration` 依赖 Node 内置模块，无法在 Tauri webview 直接 import** | 接《剩余任务交接报告》A-8 日记/日程，想把这两个"建好的模块"接线到设置页 UI 或 `scheduleManager`（webview 侧） | 实测：`dailyJournal.ts` 顶层 `import { existsSync } from 'fs'` 且构造即 `mkdir`（Node API）；`calendarIntegration.ts` 顶层 `import { exec } from 'child_process'` 且 `promisify(exec)` 在模块加载即执行 → 浏览器 bundle 中 `fs`/`child_process` 为 undefined → 加载即 `TypeError`；二者全仓除自身外无 importer（真·孤岛） | ① 日记在 webview 用安全实现：`chatStore.getState().getMessages()` 取今日对话 + `lib/i18n` 的 `formatDate`/`formatDateTime` + Blob 下载 Markdown（见 `components/JournalPanel.tsx`），绝不 import `dailyJournal`；② 日历用 `scheduleManager` 的 `CalendarSourceAdapter` 插件接口（webview 安全），`calendarIntegration` 仅作 **Node 侧参考适配器**，需经 Tauri 命令桥接后在 webview 调 `registerCalendarSource` 注册；③ 不要为"接线"而假 import Node 模块 | 2026-08-27 |
| 31 | **lib 模块静态 import `node:fs` / `node:path` → 进不了 webview bundle** | 把 `spriteAtlasBuilder.ts`（孤岛模块）接入前端导出链路：它顶部 `import { join } from 'path'` + `import { writeFile } from 'fs/promises'`，`build()` 里直接写盘 | 该模块因此**永远无法被前端引用**（一 import 就炸），成了"看起来完整、实际死着"的孤岛；同类问题在 `dailyJournal.ts`/`calendarIntegration.ts`（见 #30）已出现过 | webview 侧模块**禁止**静态 import Node 内置模块。落盘动作交给调用方：库只负责算出数据（如 `serializeMetadata()` 返回 JSON 字符串），浏览器侧用 Blob + `a[download]` 下载，Node 脚本侧自己 `fs.writeFile`。**判断口诀：lib/ 下出现 `import ... from 'fs'` 就是这个模块没被真正接通的信号** | 2026-08-28 |
| 32 | **Dedicated Worker 里没有 `requestAnimationFrame` 也没有 `document`** | `webgl.worker.ts` 的渲染循环直接调 `requestAnimationFrame(render)`；`GPUParticleSystem.createDefaultTexture()` 用 `document.createElement('canvas')` 生成默认粒子纹理 | Worker 中 `requestAnimationFrame` is not defined、`document` is not defined → 一启动就抛错，整条 Worker 渲染链路实际从未跑通过 | ① Worker 无 VSync：帧循环改用 `setTimeout(render, 1000/60)` 驱动，并保留 `stopRenderLoop()` 以便 `destroy()` 时清理；② 生成离屏画布时优先 `OffscreenCanvas`，不存在再回退 `document.createElement`，两者都没有就跳过纹理创建；③ 顺带核对时间单位：`GPUParticleSystem.update(deltaTime, ...)` 入参是**秒**，Worker 里按毫秒传会差 1000 倍 | 2026-08-28 |
| 33 | **Cubism 官方 physics3.json 与本仓内部 Physics3Config 结构不兼容** | `Live2DPysicsParser.loadConfig()` 直接 `JSON.parse` 后当 `{ version, groups: [...] }` 用；喂进社区角色的官方文件（`{ Version, PhysicsSettings: [...] }`） | `config.groups` 为 `undefined` → `this.config.groups.length` 抛 TypeError，且解析出的结构与官方字段（Input[].Source.Id / Output[].Destination.Id / PhysicsDetail.Gravity）对不上 | 在 `loadConfig` 前加**归一化层** `normalizePhysicsConfig()`，同时接受「内部简化格式」与「官方格式」；映射要点：输入权重 `Input[].Weight/100` → `input.scale`；输出 `Output[].Scale × Weight/100` → `output.scale`；**官方坐标系 y 轴向上为正（Gravity.Y 常见为 -1），本模块 y 轴向下为正，必须取反** | 2026-08-28 |
| 34 | **数值模拟类代码的 bug 靠"不抛错"测不出来** | `PendulumSimulator` 的摆动方程写成 `(g/L)·sin(θ − gravityAngle)`，在平衡位置 θ=0 处没有回复力 | 单测全绿（函数正常返回数字、不抛异常），但物理行为完全错误：摆锤被推离零点、**稳定在 ±90°**（表现为装饰品永远歪着不回正）；只有断言"从偏转位置释放后应衰减回 0 附近"才能发现 | 涉及物理/数值的模块，单测要断言**行为趋势而非仅仅不崩溃**：给一个初始偏转 → 跑 N 帧 → 断言收敛到平衡点附近、过程有界不发散、外力方向与偏转方向一致 | 2026-08-28 |
| 35 | **同名类重复：一份可用、一份在调不存在的 Rust 命令** | `src/lib/miniMode.ts` 与 `src/lib/miniModeManager.ts` **都导出 `MiniModeManager`**，且两份都零 importer | `miniModeManager.ts` 版 invoke 了 `switch_mini_mode` / `set_window_*` / `resize_window` 共 5 个 Rust 端并不存在的命令（这些名字被登记在 `ipcContract.test.ts` 的"待实现"排除列表里蒙混过关），一旦接进 UI 就是"点了没反应" | ① 新增 IPC 调用前先跑 `ipcContract.test.ts`（它扫描全仓 `invoke('xxx')` 核对 Rust 侧是否注册）；② 能用 Tauri 官方 window API（`setSize`/`setPosition`/`outerPosition`）做到的，**不要**再包一层自定义 Rust 命令；③ 发现重复实现时删除不可用的那份，并同步把对应命令名从 ipcContract 的排除列表移除 | 2026-08-28 |
| 36 | **同一个命令在两处调用，期望的返回格式不一致** | `take_screenshot` 被 `visionPerception`（期待 `{image_data,width,height}` 对象）和 `visualPerception`（期待 base64 **字符串**）各 invoke 一次，参数也不同（region vs maxWidth+quality） | Rust 端实现时只能选一种格式：选对象格式后，visualPerception 拿到对象直接当 base64 用 → 上游静默坏掉 | 实现一个"计划中"命令前，**先全仓搜索该命令名的所有 invoke 调用点**，核对每处的参数与返回类型约定；不一致时统一语义（信息全的一方为基准）并适配另一处，而不是迁就先写的那个 | 2026-08-28 |
| 37 | **windows crate 接口藏在子 feature 里** | `IAudioEndpointVolume` 位于 `Win32::Media::Audio::Endpoints` 模块，只开 `Win32_Media_Audio` feature 仍报 unresolved import | COM 绑定编译不过，报错位置在 use 行，容易误判为 API 名写错 | windows crate 的 feature 粒度到模块级：`Media::Audio::Endpoints` 需要单独的 `Win32_Media_Audio_Endpoints`。遇到 unresolved import 先对照 [docs.rs](https://docs.rs/windows) 找到类型真实模块路径，再补对应 feature | 2026-08-28 |
| 38 | **`Option<HWND>` 不是 `Param<HWND>`（windows 0.58 句柄参数）** | `GetDC(None)` / `ReleaseDC(Some(HWND::default()), hdc)` 编译报 E0271/E0277 | GDI FFI 调用编译不过 | windows 0.58 的部分 API 已从 `Option<HWND>` 收紧为直接 `HWND` 参数：空句柄用 `HWND::default()`，不要套 Option（与 Tauri API 风格相反）。`[u16; 260]`（如 `PROCESSENTRY32W.szExeFile`）也没有 Display/ToString，须用 `String::from_utf16_lossy` | 2026-08-28 |
| 39 | **"已接线"但驱动定时器从未 start —— 整条链路实际是断的** | `proactiveSpeak` 的 `start()` 全仓零调用（只有 `usePetTimers` 订阅了 `onProactiveSpeak` 事件） | 主动说话功能"看起来完整"：有检查、有订阅、有生成逻辑，但定时器从未启动 → 用户永远看不到宠物主动开口；靠代码审阅/测试都发现不了（不抛错、不报错） | ① 排查"事件驱动型功能"时，除找 importer/订阅方外，**必须搜 `<管理器>.start(` 的调用方**——订阅者 ≠ 驱动者；② 接线时把 `start()`/`stop()` 与订阅放同一 useEffect（start 依赖订阅建立后 `listeners.size > 0` 才会持续自调度，见 `scheduleNextCheck`）；③ 验收标准里加一条"空闲 N 分钟后真的触发"的运行时断言 | 2026-08-28 |
| 40 | **vi.mock 工厂内引用外部变量必须 vi.hoisted** | 在测试文件里写 `const mockX = vi.fn(); vi.mock('../m', () => ({ f: mockX }))` | vitest 把 vi.mock 提升到 import 之前，工厂执行时 `mockX` 未初始化 → ReferenceError / 工厂拿到旧绑定 | 需要从工厂外引用 mock 函数时用 `const { mockX } = vi.hoisted(() => ({ mockX: vi.fn(() => false) }))`，再在工厂里返回 `mockX` 本身；工厂参数不要用 `(...args: unknown[]) => mockX(...args)` 展开（spread 到 unknown[] 会触发 TS2556，直接返回函数引用即可） | 2026-08-28 |
| 41 | **同一份交接报告会过时——接线"核实"先于"接线"** | 《剩余任务交接报告》判定 A-1/A-2/A-3/A-5 为孤岛，但 PetWindow 实际早已完整接线（look/hiddenStateManager/useDockVisualFeedback/silentMode 全部有消费） | 按报告盲目重复接线 → 产生同名函数/按钮/import 冲突（tsc TS2300/TS2393），甚至踩到"更完整的既有实现" | 处理"孤岛接线"类任务时，**第一步先 grep 目标符号在运行链路文件的全部出现**（PetWindow.tsx 这种大文件），确认是否已接线再动手；报告标注的日期（2026-08-27）早于代码演进时尤其要核实 | 2026-08-28 |
| 42 | **假测试会掩盖真 bug —— 数据驱动模块不能 mock 掉数据层** | `i18n.test.ts` 把 i18next 整个 mock 成 `t: (key) => key`，只断言"返回类型是字符串"，测试全绿 | 掩盖了一个影响全部带参文案的静默 bug：字典用单花括号 `{amount}`，而 init 未配 `interpolation.prefix/suffix`（i18next 默认 `{{ }}`）→ 所有插值**原样输出 `{amount}`**。测试完全测不出来 | ① 识别"数据驱动模块"（i18n 字典、解析器、配置表、映射表）：mock 掉数据层 = 放弃全部真实覆盖，**至少要有一份用真实实例的测试**；② 断言要落在"内容"上（如"包含 10 且不包含 {amount}"），而非"类型/被调用"；③ 对 i18n 这类模块，补一份覆盖全部语言的插值/格式化测试成本很低，收益极高 | 2026-08-29 |
| 43 | **引入新模块依赖会连锁暴露既有测试的 mock 缺口** | 新增 `cultureEmoji.ts` 依赖 `settingsStore`，而 settingsStore 默认值调用 `getDefaultCharacter()`；`usePetTimers.test.tsx` 的 `vi.mock('../../lib/characters')` 只提供了 `getCharacter` | 测试报 `No "getDefaultCharacter" export is defined on the mock`，且报错模块看起来与改动无关，容易误判为随机失败 | ① 报错 `No X export is defined on the Y mock` → 直接补 mock 导出（这是测试基建不完整，不是产品代码 bug）；② **在 hook/组件里新增 store 依赖前，先想清楚这条依赖链会拉进哪些模块**（Zustand store 的默认值常常在模块加载时就执行），受影响的测试可能需要同步补 mock | 2026-08-29 |
| 44 | **Android 11+ 包可见性：`resolveActivity()` 恒为 null（跨项目同型坑）** | 安卓真机（targetSdk 30+）上任何用 `intent.resolveActivity(pm)` 预判"有没有应用能打开"的代码，而 `gen/android/app/src/main/AndroidManifest.xml` 无 `<queries>` 声明。本仓命中点：wry 生成的 `RustWebChromeClient.kt` 用其预判 IMAGE_CAPTURE/VIDEO_CAPTURE（DraftPeek 社交链接同坑，其 AGENTS.md Gotcha #26） | 真机上恒判"无相机应用/无法打开该应用"，但系统其实能打开；单测/lint/低版本模拟器均测不出 | ① manifest 声明 `<queries>`（VIEW http/https + 用到的 MediaStore capture action）；② 自己写的跳转代码不要用 `resolveActivity` 当判据，直接 `startActivity` 捕获 `ActivityNotFoundException`；③ 生成代码（wry/Tauri gen）不宜手改时，优先用 manifest 声明修复上游预判的可见性 | 2026-08-29 |
| 45 | **PendingIntent 指向自家 scheme 却没注册 intent-filter（半接线功能）** | `PetWidgetProvider` 发 `spiritpal://feed|open_chat|pet` 的 ACTION_VIEW PendingIntent，但 manifest 无对应 `<intent-filter>`；前端 `parseWidgetDeepLink()` 有解析器、有注释宣称"Widget 点击通过 Deep Link 回到主应用"，却零调用方 | 点击小组件按钮静默无反应（Activity 无法解析）；代码审阅/测试都发现不了——每个环节"看起来都有"，缺的是环节之间的接线 | 声明自家 deep link 必须三件套齐查：① manifest intent-filter（scheme 注册）② 系统入口（onNewIntent/getIntent → 事件/命令转发前端）③ 前端消费方（解析 + 动作）。修一处前先 grep scheme 在 manifest/Rust/前端的全部出现，缺一即在文档标注"半接线"防止误判已完成 | 2026-08-29 |
| 46 | **别名解构 invoke 绕过 IPC 契约扫描器，不存在的命令带病入库** | `widgetState.handleWidgetDeepLink` 用 `const { invoke: inv } = await import('@tauri-apps/api/core')` 后 `inv('feed_pet_from_widget')`；ipcContract.test.ts 的采集正则只认字面量 `invoke(`，且扫描目录直接跳过 `mobile/`。结果 4 个动作分支里 3 个调用的 Rust 命令（feed_pet_from_widget / open_chat_window / open_settings_window）根本不存在，107 项契约测试全绿 | 契约测试通过 ≠ 命令存在——写法换个马甲就隐身；运行时表现为动作静默失败（reject 被吞） | ① 扫描器已补别名采集（`const { invoke: X }` → `X('cmd')` 一并入检）并移除 `mobile/` 目录豁免；② 新增 invoke 一律优先复用前端既有 store/lib（本次 feed 改走 `petStore.useItem`，含背包消耗/好感度/buff 全套语义，无需新 Rust 命令）；③ 注意扫描器不识别注释里的 `invoke('...')` 字样，文档示例措辞要避开 | 2026-08-29 |
| 47 | **移动端窗口 label 是 "main"，capability 没纳入 → 安卓端所有受权限门控的调用被静默拒绝** | 三个 capability 文件（default/chat-window/settings-window）的 `windows` 只列了桌面窗口标签 `pet-window`/`chat-window`/`settings-window`；但 Tauri 移动端单窗口的 label 是 `"main"`（webview 也叫 main）。桌面三窗口各自有权限，移动端 "main" 却一个 capability 都不匹配 | 安卓端 `event.listen not allowed on window "main"`（deep-link 监听器、windowEventBus.on 全部注册失败）、`sql.load not allowed on window "main"`（SQLite 持久化 / 宠物存档 / 迁移全废）、store/notification 同理。表现是"功能在桌面正常、上安卓就哑"，且因为都是 reject 被 catch 吞掉，UI 不报错、极难定位 | ① 移动端单窗口必须把 `"main"` 加进 capability 的 `windows` 列表（本项目三个文件都加，取权限并集）；② 排查"安卓专属失效"先用 WebView 远程调试（`adb forward tcp:9222 localabstract:webview_devtools_remote_<pid>` + 原生 WebSocket 跑 CDP `Runtime.evaluate`）看 console，权限拒绝错误会以 `not allowed on window "main"` 直接现形；③ 桌面用多窗口分权、移动端合并为单窗口的架构，capability 白名单必须显式覆盖 "main"，否则等于移动端裸奔 | 2026-08-29 |
| 48 | **Win32 FFI 命令的真机三 bug —— 纯逻辑单测全绿但运行时全坏（UTF-8/headless CI 测不出）** | 真机冒烟 A 段命令此前只有纯逻辑单测（通配符/截断/白名单常量），从未真正调用过 Win32 运行时路径。补 `tests/test_system_runtime.rs` 在真实中文 Windows 上一调，当场炸出三个 bug：① `get_running_processes` 用 `from_utf16_lossy(&szExeFile)` 把 `[u16;256]` 定长缓冲整个转换，进程名带一长串 `\0` 尾巴，前端 `includes('xxx.exe')` 全落空；② `execute_command` 用 `read_to_string` 读子进程输出，中文 Windows 的 tasklist/ipconfig 首行是 GBK 中文表头（非法 UTF-8）→ 读取报错被 `let _ =` 吞掉 → 返回空串，ASCII 的进程名/IP 全丢；③ `execute_command` 白名单只校验首 token，`tasklist & del evil.txt` 被 cmd /C 链式执行后半段破坏性命令 —— 只读白名单形同虚设 | 三个 bug 单元测试都发现不了：① 需要真 PROCESSENTRY32W 定长缓冲；② 需要非 UTF-8 控制台 locale（CI 是 UTF-8）；③ 需要真跑 cmd。现象分别是"进程检测永远空""命令输出永远空""安全边界可绕过" | ① 定长 UTF-16 缓冲转字符串必须在第一个 NUL 截断（`iter().position(|&c| c==0)` 再切片）；② 读子进程控制台输出用 `read_to_end` + `String::from_utf8_lossy`，别用 `read_to_string`（GBK 会整体失败）；③ 命令白名单必须同时拦截 shell 元字符 `& \| > < ^ ; \` $ ( ) % !` 等，只查首 token 不够；④ **凡 Win32/COM/子进程 FFI 命令，必须补一个在真实 Windows 上直接调 `pub async fn`（`tauri::async_runtime::block_on` 包裹）的集成测试**，纯逻辑单测不算数 | 2026-08-29 |

| 49 | **目录重构后测试基建的 mock 相对路径不更新 → 全量测试大面积失败（Worker is not defined）** | split-lib 把 src/lib 平铺文件移入 ai/data/memory/nurture/render/system 子目录并重写业务 import，但 src/test/setup.ts 的 vi.mock("../lib/vectorWorker?worker") 与 vectorSearch.test.ts 的 vi.doMock("../vectorWorker?worker") 仍是旧相对路径，指向已不存在的顶层文件 | 重构后 pnpm test 一次爆 47 文件 / 297 用例失败，绝大多数报 ReferenceError: Worker is not defined（worker mock 未命中 → 真实 new Worker 在 jsdom 不存在） | 目录重构必须同步扫描所有测试基建（setup.ts 全局 mock、各测试文件 vi.mock/vi.doMock 相对路径、alias 是否切换）；用 rg 全量核对 ?worker 与 vi.mock 相对路径；漏一处就是整片测试红 | 2026-09-04 |
| 50 | **rustup 工具链 manifest 损坏时 cargo test 报 missing manifest / not applicable，需换国内镜像强制重装** | 并发会话操作 rustup 或安装中断后，1.98.0 报 missing manifest（cargo 可用但 rustc 不可用），+stable 报 cargo/rustc not applicable，rustup component add 也失败 | cargo test 编译即挂；cargo --version 能返回但 rustc 不可用，极具迷惑性 | 用 rustup +<ver> --version 分开诊断；官方源中国环境易卡住，改用 RUSTUP_DIST_SERVER=https://rsproxy.cn RUSTUP_UPDATE_ROOT=https://rsproxy.cn/rustup rustup toolchain install <ver> --force；若 rust-toolchain.toml override 未生效，用 cargo +<ver> test 显式指定绕过 | 2026-09-04 |
| 51 | **git clean / checkout 会毁掉 untracked 文件与会话内未提交工作** | 目录重构前想"恢复干净状态"，对 src/ 跑了 `git checkout -- src/` 再 `git clean -fd src` | git clean 删掉未跟踪的测试文件（dualBrain/ipcSecurity/pathTraversalGuard/uploadMagic.test）与当轮生成的 migrations/types.ts；checkout 又还原了本会话 S1 的断环成果；工作区还混有并发 Agent 的未提交工作，破坏范围难以评估 | 任何需要"还原"的场景先 `git status` 盘点 untracked/M 再动手；**禁用 git clean -f / checkout -- / reset --hard**；确需还原只对属于自己改动的单个文件 `git restore <file>`；被删 untracked 文件只能按原测试意图人工重写 | 2026-09-04 |
| 52 | **vitest fake timers 全集合 + advanceTimersByTimeAsync 遇深层 await 链挂起到真实超时** | 单测用 `await vi.advanceTimersByTimeAsync(1000)` 想加速被测代码内部 `setTimeout(1000)`，但中间隔了多层 await Promise | "Test timed out in 5000ms"，multimodalLLM.test 5 个用例同时 5s 超时；顺序/并发运行行为不一致 | 改用 `vi.useFakeTimers({ shouldAdvanceTime: true, advanceTimeDelta: 20 })` 虚拟时钟自动推进，别再用手动 advanceTimersByTimeAsync 与深层 await 链博弈；runAllTimersAsync 在 jsdom+worker 下同样不可靠 | 2026-09-04 |
| 53 | **PowerShell 的 git commit -m 传多行 here-string 会被拆义** | `$msg = @'...'@` 构造多行提交消息再 `git commit -m $msg` | git 报 "did not match any file(s) known to git"，提交根本没生成 | 多行提交消息用 `git commit -F <临时文件>`（Set-Content -Encoding utf8 后 -F 读取），或单行 `-m`；不要用 -m 接多行变量 | 2026-09-04 |
| 54 | **多 Agent 并发写同一工作区：文件被改写/删除、测试结果随运行窗口漂移** | 长任务执行期间另一 Agent 在同时工作（运维整改、测试探针调试） | 早前 Read 的测试文件内容被另写（isolate3→isolate4）、_debugTimer.test.ts 突然消失、mtime 持续变化；全量测试失败集每次运行不同（mcpServer/visualMemory/multimodalLLM 轮番失败但单跑全过） | 关键操作前核对 git status 与文件 mtime；出现文件消失/内容被覆盖立即停下确认并发方；测试结论以连续 2 次稳定运行结果为准；提交只用白名单 add 自己负责的文件 | 2026-09-04 |
| 2026-09-04 | rustup 工具链 manifest 损坏（rustc not applicable） | rustup component 显示 up to date 但 rustc 不可用 | 用 RUSTUP_DIST_SERVER=https://rsproxy.cn 重装对应 channel 修复；勿改 rust-toolchain.toml | 
| 2026-09-04 | vi.mock('fs/promises') 未命中 node:fs/promises 业务 import | 测试 real read 抛 ENOENT | mock specifier 必须与业务 import 前缀完全一致（node: 前缀也匹配） |
| 2026-09-04 | Playwright page.goto 10s load 超时（dev 多窗口重部件） | accessibility 测试 flaky 失败 | waitUntil=domcontentloaded + waitFor #root；navigationTimeout 放宽 30s |
| 2026-09-04 | 工作区存在大量 WIP 与任务改动同文件交错 | 无法按片段分离提交 | 按模块分批 add 显式文件并在提交说明标注随带 WIP，避免 add -A |

| 51 | **图集行名 ≠ 动画 ID：spiritpal_react 拿 ANIMATION_ROWS 行名比对 ANIMATION_CATALOG，除 idle 外全部误判 Unknown reaction** | OPENPETS_REACTION_MAP 的值（waiting/running/review/jumping/failed/waving）是 ANIMATION_ROWS 图集行名（精灵图集 9 行），而 spiritpal_react 把它们当 ANIMATION_CATALOG 动画 ID（thinking/editing/testing/success/error/celebrating 等）做 validIds.has 校验 | MCP spiritpal_react 工具除 idle 外所有反应都返回 "Unknown reaction"（isError），外部 AI 以为宠物不响应；单测覆盖了该
行为反而固化了 bug | ① 区分两类映射：ANIMATION_ROWS（图集行，驱动精灵帧）与 ANIMATION_CATALOG（动画 ID，驱动动画系统），不可混用；② 反应→动画映射用显式 REACTION_ANIMATION_MAP（反应名→动画 ID），状态类反应回退 idle；③ 单测应断言"合法反应能触发动画事件"而非"合法反应返回错误" | 2026-09-04 |
| 52 | **块注释内出现 `*/` 序列 → TS 报 "Unterminated regular expression"** | 写注释"schema_*/dirty_data_registry"（本意是通配符示意），`*/` 提前终止了 `/* */` 块注释，后续代码被当成正则/非法 token | tsc 报 TS1127 Invalid character / TS1161 Unterminated regular expression，定位在注释相邻行，容易误判为文件损坏 | TS 块注释里**禁止出现 `*/` 子串**（含通配符示意），改写为"schema 系列"或拆分措辞；写完含 SQL/通配符的注释立即 tsc 校验 | 2026-09-04 |
| 53 | **vi.stubGlobal('URL', {...URL}) 会破坏 SSRF 校验的 `new URL()`，网络层用例报 "URL 格式无效"** | 测试为 stub createObjectURL 而 `vi.stubGlobal('URL', { ...URL, createObjectURL: v.fn() })`；实现把网络请求换成 safeFetch / SSRFProtector.validate（内部 `new URL(url)`）后，URL 构造器被普通对象替换 → 校验抛 TypeError 被捕获为 "URL 格式无效" | 改网络层后既有 fetch stub 用例全部失败：SSRF 防护: URL 格式无效；错误信息与真实 URL 无关，极难定位 | ① 单测隔离网络层一律 mock `safeFetch`（vi.mock 模块出口 + `mockResolvedValueOnce`），不要 stub 全局 fetch/URL；② 确需 stub createObjectURL 时避开构造器：`URL.createObjectURL = vi.fn()` 并在 afterEach 恢复；③ 改动涉及网络出口（safeFetch/httpproxy）时，先 grep 各测试是否 stubGlobal（'URL'/'fetch'） | 2026-09-04 |
| 55 | **trait 对象经 Arc 进 spawn_blocking：dyn Trait 缺 Sync 报 E0277** | keychain.rs 整改把密钥访问抽成 	rait SecretStore（声明 Send + 'static），命令用 Arc<dyn SecretStore> 放入 	auri::async_runtime::spawn_blocking | 编译报 (dyn SecretStore) cannot be shared between threads safely，报错点在闭包捕获处而非 trait 声明处，极难定位（提示词甚至断言这是「既有编译失败」） | ① Arc<T>: Send 要求 T: Send + Sync，跨线程共享的 trait 对象一律声明 Send + Sync + 'static；② 改 trait 能力边界时先想它会以什么容器进入线程（Arc/Box+spawn）| 2026-09-04 |
| 56 | **Cargo git 依赖 
ev 与 ranch 互斥，不能同时 pin** | M4 给 tauri-nspanel 加 
ev = "a3122e8…" 时保留了 ranch = "v2.1" | cargo metadata 报 dependency specification is ambiguous: Only one of branch/tag/rev is allowed | Cargo 的 git 依赖 pin：
ev（commit）与 ranch/	ag 三选一；要防内容漂移就只用 
ev（锁定确切 commit），要跟随分支就只用 ranch，不可共存 | 2026-09-04 |
| 57 | **setup 拆分后出现「函数体重复 + 一方 dead_code」——哪份是最新实现要靠运行路径判断** | lib.rs 中 setup_desktop_app 与 run() 的 setup 闭包内联块几乎重复（窗口/托盘/快捷键），setup_desktop_app 标 #[allow(dead_code)] 但实际从未被调用；两版在 focus-mode/chat 窗口创建方式上还有行为差异 | 无法用静态分析确定哪份是「最新想要的」；直接删旧版可能丢行为，直接删内联版又把 dead 代码激活 | 重构「疑似重复」时：① 先看谁被真正调用（运行路径 = 事实源）：内联块在 run() 里被执行 → 以它为基准迁移；② 迁移后删除 dead 函数并去掉 allow(dead_code)；③ 对比两版差异逐项确认（本例 focus-mode 行为以运行版为准）| 2026-09-04 |
| 58 | **PowerShell 管道吞掉原生命令退出码：cmd | Select-String 后 $LASTEXITCODE 不可信** | 用 cargo check 2>&1 | Select-String ...;  判断编译结果 | 管道后 $LASTEXITCODE 反映的是管道中最后一条原生命令（cargo）状态——但 Select-String 无输出时仍返回成功，且 EXIT 值曾被误读为 0 导致「以为全绿」 | 判断编译/测试成败：① 不用管道时直接看 $LASTEXITCODE；② 需要过滤时把输出重定向到文件再过滤（cmd | Out-File tmp; Select-String tmp），或观察命令自身 stderr 的 error 字样；③ 重要门禁（提交前）一律跑不带管道的原始命令 | 2026-09-04 |
| 59 | **Tauri 自定义命令不经过 capability ACL；plugin-sql 无细粒度 SQL 权限** | 报告评估误以为可把 sql:allow-execute 换成 select/insert/update/delete 细粒度项 | 实测 tauri-plugin-sql v2 只暴露 execute 内部命令（JS 的 execute/select 共用），capability 只有 allow-execute/load/close，无按 SQL 语句类型的细粒度授权；自定义 tauri::command 也根本不经过 capability | ① SQL 收口整体下沉 Rust 语义命令（sqlite.rs sp_*），capability 删除 sql:*；② 高敏自定义命令另加 window_gate.rs 运行时窗口校验 | 2026-09-04 |
| 60 | **Tauri 命令无法注入 Option&lt;Window&gt;：墙内调用与 Rust 内部直调要拆两层** | 为兼容内部调用把敏感命令参数写成 window: Option<tauri::Window> | 编译报 tauri::Window: serde::Deserialize 未实现 / Option<Window> implements CommandArg 失败 | injectable 参数只有固定形态；需要「WebView 调用门禁 + Rust 内部直调」双路径时，拆成「命令包装层(带 Window) + 内部核心函数(不带)」两层，勿用 Option 硬套 | 2026-09-04 |
| 61 | **cmd.exe / sh -c 整串传参 + 白名单首 token = shell 内置可读任意文件** | execute_command 白名单含 type/dir/cat/echo，走 cmd /C 整串执行 | type 读任意文件、cat 读 /etc/shadow，不含任何元字符即绕过元字符黑名单 | 显式「命令→可执行文件+固定参数」映射（无 shell 解释器），移除 type/dir/echo/ver/cat/ps 内置读者；元字符黑名单仅兜底；单测断言内置读者被拒 | 2026-09-04 |
| 62 | **perf 子脚本漏 import saveResultJson：测试本身 PASS 但 run-all 判 FAIL** | 迁移重构把 perf/*.mjs 从 _helpers.mjs 引用的函数拆走/改名后，cold-start.mjs 与 memory-usage.mjs 仍调用 saveResultJson 但 import 列表未同步 | run-all.mjs 汇总显示「实测 732ms < 2000ms 却 ❌ FAIL」；单独跑子脚本到 saveResultJson 处崩 ReferenceError（exit 2） | ① 子脚本 run-all 判定依赖「子进程退出码」，FAIL 先单独跑子脚本看是否 ReferenceError 类崩溃；② perf 迁移后批量 `rg -l "xxx(" perf -g "*.mjs"` 对比 `rg -l "import.*xxx" ...` 找漏导；③ 提交前跑一遍 node perf/run-all.mjs | 2026-09-04 |
| 63 | **工作区偶发「单行写入丢失」：Edit 返回成功但磁盘字节未变** | 大重构/多会话并发期间对同一工作区连续编辑多个文件时，偶发个别 Edit 报告成功、磁盘内容却仍是修改前旧值（实测双向检查可复现） | 类型检查出现与「刚写入」矛盾的报错（如 vitest 通过但 tsc 报 import 缺失；describe 标题仍是旧值）；git status 不符预期 | ① 关键路径（测试/门禁文件）编辑后立即 `Get-Content -TotalCount N` / `rg -n` 抽查落盘，不一致立刻重做该编辑并复验；② 改动越少越好、分批提交硬化；③ 遇到「工具口径矛盾」先怀疑文件被并发还原，先查 git status 全量快照再继续 | 2026-09-04 |
| 64 | **Tauri 插件版本一致性门禁：Rust crate 与 npm 包必须同版本** | Cargo.toml 写 `tauri-plugin-notification = "2"` 解析到 2.3.3，package.json 写 `^2.0.0` 解析到 2.4.0 后执行 `pnpm tauri build` | CLI 报 `Found version mismatched Tauri packages: tauri-plugin-notification (v2.3.3) : @tauri-apps/plugin-notification (v2.4.0)` 并终止构建，发版全被阻断 | 任何 Tauri 插件改动/升级后，`pnpm tauri build` 前先核对两端版本：`cargo tree -p tauri-plugin-xxx` 与 `node -p "require('@tauri-apps/plugin-xxx/package.json').version"`，不一致时 `cargo update -p tauri-plugin-xxx --precise <npm版本>`（改 Cargo.lock，勿改 Cargo.toml 约束）| 2026-09-10 |
| 65 | **updater endpoints 的 GitHub 仓库名必须与实际 remote 一致，否则 updates.json 永远 404（漏更新）** | tauri.conf.json 的 endpoints 沿用立项时假设的 `spiritpal/spiritpal-app`，实际远程是 `ReSerendipity/SpiritPal` | 客户端检查更新请求 raw.githubusercontent.com 404，用户永远收不到新版本提示（静默失效，且不报错） | ① endpoints 的 owner/repo 必须与 `git remote -v` 的实际值一致；② 配置后实测一次 `curl -sI https://raw.githubusercontent.com/<owner>/<repo>/main/updates.json` 应返回 200；③ 发版链路末尾强制生成并提交 updates.json（见 SOP-5）| 2026-09-10 |
| 66 | **Edit 工具无法匹配 CRLF 文件的多行 old_string** | Windows 仓库内 .json/.yml 等以 CRLF 存储，用多行 old_string 做精确替换 | Edit 返回失败（条件不满足），多行替换反复重试无效 | 对 CRLF 文件改用 Python 脚本（`io.open(..., newline="")` 读写保持 CRLF）或 Write 全量重写；先 `([regex]::Matches($c,"`r`n")).Count` 判断换行风格再选工具 | 2026-09-10 |
| 67 | **测试框架大版本升级会改变覆盖率口径——阈值需按新口径重校准，否则门禁假红** | vitest 3.2.7 → 4.1.11（安全修复，无 3.x 修复版，不可回退）后未复核覆盖率门禁 | CI 前端 job 从 `cc0885a` 起连续红：`ERROR: Coverage for lines (46.64%) does not meet global threshold (48%)`、functions 43% < 60%、branches 38.45% < 50%。vitest 4 的 v8 provider 改为 AST 感知重映射，对零覆盖模块的 function/branch 计数口径与 3.x 不同 | ① 判据先行：确认「转红区间内 src/ 生产代码零改动 + 测试全过」→ 归因口径变化而非质量退化；② 按新口径重校准阈值并留 ~0.5pp 余量，同步 `docs/agents/QUALITY_CONTRACT.md`（其旧值 lines 40/functions 70 亦早已过期）；③ 教训：依赖大版本升级（vitest/jest/tsc 等）后必须复核所有「按百分比」的门禁口径，不能只看测试是否全过 | 2026-09-11 |
| 68 | **pnpm 大版本有 Node 下限——CI 矩阵里保留过低 Node 版本会结构性红** | `pnpm/action-setup@v4` 锁 version 11，CI 前端矩阵仍含 Node 20 | Node 20 job 在 `Install Node.js 20` 后即失败：`warn: This version of pnpm requires at least Node.js v22.13` / `ERR_UNKNOWN_BUILTIN_MODULE: No such module: node:sqlite`（pnpm 11 用到 Node 22 内建 sqlite） | 升级 pnpm 主版本后同步收紧 CI 的 `node-version` 矩阵（本次 [20,22] → [22]）；判据是 `pnpm --version` 与 lockfileVersion 的实际要求，而不是 package.json 里可能缺失的 engines 字段 | 2026-09-11 |
| 69 | **Tauri capability 按「窗口 label」授权，但前端组件可能挂在多个窗口——授权面要按实际调用面给全** | UpdateNotification 只在 settings-window 授 updater 权限，却在 App.tsx 根节点挂到 pet/chat/settings 全部窗口；30s 自动检查在主窗口必报 `Command plugin:updater\|check not allowed by ACL`（v0.1.0 实发版本即带此缺陷） | 用户在宠物主窗口稳定看到「更新失败」弹窗，错误指向 ACL 而非网络；单测（jsdom 无 ACL）全绿发现不了 | ① 新增会 invoke 插件命令的「全窗口挂载」组件时，逐个 capabilities/*.json 核对调用面（pet/chat/settings 三窗口 + main 兼容 label）；② capability lint 的 invoke/插件实现校验只证明「某处授权过」，证明不了「每个调用窗口都授权」，需人工按挂载点复核；③ download-and-install 还要同时授 process:allow-restart，store 类后台能力（windowPositionMemory）同理补 load/get/set；④ 验收方式：真机 dev 构建 F12 控制台直接 invoke 原命令，ACL 缺口立即现形 | 2026-09-11 |
| 70 | **透明点击穿透宠物窗上做 GUI 自动化：物理右键/拖动不可靠，改走 debug DevTools 控制台真实事件链** | 用 cu/鼠标自动化在 SpiritPal 宠物窗上右键开动作面板 | 窗口 set_pet_click_through 按像素 alpha 100ms 轮询切换 WS_EX_TRANSPARENT，光标一到透明区后续点击直接穿透；面板打开后 600ms 防抖收起；物理 cu 右键/焦点激活的时序极难命中 | ① 需要「真实应用、真实事件」的 GUI 验证：`pnpm tauri dev` 起 debug → F12（SetForegroundWindow+SendKeys 比 cu press_key 可靠）开 WebView2 DevTools → 控制台执行真实 DOM 事件（dispatchEvent contextmenu、querySelector 按钮 .click()）、`__TAURI_INTERNALS__.invoke()`、window.dispatchEvent；计时用页内 performance.now()；② DevTools 首次粘贴要手输/真键 `allow pasting`（本地化界面也是英文短语）；③ WEBVIEW2_ADDITIONAL_BROWSER_ARGUMENTS 开 CDP 在 debug/release 均无效，别再试；④ 注意首迎 CharacterSelector 阶段没有 data-sprite 元素 | 2026-09-11 |
| 71 | **GitHub 加速器（Watt Toolkit/Steam++）hosts MITM 会让 rustls 系 HTTP 客户端静默失败** | 本机 hosts 把 github 全域指 127.0.0.1、Steam++.Accelerator 监听 443 用本机 CA 代签时，跑 tauri-plugin-updater 检查更新 | WebView2/PowerShell（走 Windows 证书库）请求同 URL 200，但 updater（reqwest + 非系统证书库）报 `Could not fetch a valid release JSON from the remote`；易误判为产品 bug | ① 排查 updater/reqwest 网络失败先查 hosts（`Select-String github $env:windir\System32\drivers\etc\hosts`）+ 443 监听进程；② 区分「OS 证书库成功但 rustls 失败」=MITM CA 环境问题；③ 产品侧失败原因必须透传到 UI（本项目已做到不静默）；④ 验证链路可临时换公网可信镜像 endpoint，取证后还原，勿提交临时改动 | 2026-09-11 |
| 72 | **规范文档技术栈/门禁描述会随代码演进漂移——AGENTS 自检⑧与 ARCH_MAP 技术栈表与实际不符** | 做「接入新宠物」调研时核对宠物渲染链路：AGENTS.md 自检⑧声称"本仓无 .pre-commit-config.yaml"，ARCH_MAP 技术栈表写"Framer Motion v11 + Lottie Web / assets/live2d/<petId>/" | 实际 `.pre-commit-config.yaml` 存在（structure-guard 自动生成 structure-guard/capability-lint 两钩子，stages 覆盖 pre-commit/pre-push）；package.json 无 framer-motion/lottie 依赖；宠物动画实为 SpriteRenderer（atlas 精灵图集/video webm/svg），Live2D 由 usePetLive2D HEAD 探测 `/pets/<id>/<id>.model3.json` 命中才启用，Cubism Core 用户自装 | 按铁律 #1 同步规则修正文档：① ARCH_MAP 技术栈行以 package.json 实际依赖 + 渲染组件为准（勿信 Lottie/Live2D 旧描述）；② pre-commit 陈述以仓库根文件为准（structure-guard 相关 workflow 会自动生成 `.pre-commit-config.yaml`，REVISION_LOG 的历史"无此文件"结论会过期）；③ 发现占位值（如 i18n en `char.feibi`='参考实现'）与死样例（public/characters/*/pet_conf.json）先全局 grep 确认无引用再修/删，同步 THIRD_PARTY_NOTICES 等提及路径的文档 | 2026-09-11 |
