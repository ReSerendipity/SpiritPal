# SpiritPal 会话交接简报（新对话基础信息）

> 生成：2026-08-29 ｜ 依据：`docs/plans/SpiritPal-下一步实施计划.md`（已同步至 B-4 完成）+ 当前仓库 HEAD `28db6cd`
> 用途：新开对话 / 新 AI 会话的起步上下文。读完本文件即可接手，无需重读全部历史。
> **更新：2026-08-29（同日复核）** —— 原 §2「批次三 ⏳ 待启动」系过期标注：A-6/A-7/A-11/B-1 及 A-12~A-15 运行时早已接线完成；C-2、C-6 已完成；P0（P0-1/2/3）已在 working tree 完成（含回归测试）。详见 §2 复核说明与 §4。

## 1. 这是什么项目

SpiritPal「桌边友」—— 跨平台 AI 桌面宠物（Tauri v2：Rust + React 19 + TypeScript）。
核心理念「简单而温暖」：情感陪伴 + 轻量生产力辅助。
详见根目录 `README.md`（含分支策略）与 `AGENTS.md`（架构与铁律）。

## 2. 完成度速览（来自实施计划）

| 批次 | 内容 | 状态 |
|------|------|------|
| 批次一 | A-1~A-5 体验接线（看看 / 贴边互动 / 记忆可视化 / 静默） | ✅ 完成 |
| 批次二 | A-8 日记日程 / A-9 i18n 收敛 / A-10 情绪引擎 | ✅ 完成 |
| 批次三 | A-6 帧缓存·图集 / A-7 抚摸粒子 / A-11 摘要·推荐 / B-1 mcpHooks | ✅ 完成（接线早于本简报，原"待启动"为过期标注，见下方说明） |
| 批次四 | B-2 自动更新 → B-3 流式加密 → **B-4 评测/基准** → C 类清理 | 🟡 B-4 完成；B-3 与 C-1~C-7 已于 2026-08-30 复核确认完成（仅 C-5 人工许可待办）；B-2 部分完成（release.yml 已就位，剩签名私钥 + updates.json 需仓库管理员） |

**B-4（记忆评测 / 性能基准 / V1-V4 真机验收）：✅ 完成，含 2 项已知阻塞（见 §3）。**

> **复核说明（2026-08-29）**：本简报生成时把"批次三"误标为 ⏳ 待启动，但代码核对显示这些功能在更早的提交（A-4/A-1 时代）就已接线：
> - A-6 帧缓存/图集：`GifToSpriteTool.tsx` 使用 `createSpriteAtlas`；`frameCache.ts` 被 `spriteMemoryManager.ts` / `trayIconRenderer.ts` 消费。
> - A-7 抚摸粒子：`PetWindow.tsx:88` 引入 `usePetParticles` 并于 `:860` 挂载。
> - A-11 摘要/推荐：`proactiveSpeak.ts` 注入推荐、`memoryBackground.ts` 触发摘要。
> - B-1 mcpHooks：`main.tsx:234` 挂载 `startMcpAppBridge()`。
> - A-12/A-13/A-14/A-15 运行时：记忆面板 `MemoryPanel`、`useDecorationPhysics`、`useMiniMode`、`src/lib/modPackager.ts`+`modManager.ts`（↔ Rust `petmod.rs`）均有真实消费者，**勿重复实现**。

## 3. 最近一轮交付：B-4 记忆召回收尾

目标：补齐记忆检索的评测与性能基准，建立可对比的回归看板。

- 检索候选池纳入工作 / 自传记忆（修复 case 30 漏召回）— `31bc13e`
- 压缩记忆保留可检索 + 查询同义词扩展 — `ca2be59`
- 拖拽帧率基准 + V1-V4 真机验收 checklist — `2102ebb`
- 移动端 deep-link / 包可见性 / Win32 命令真机三 bug — `ec9e756`（上一轮 AI）
- 记忆检索 P95 基准 `memoryRecall.bench.test.ts`（p95≈0.64ms，可达检索池 300 条）
- 30 条召回评测集 `memoryRecall.eval.test.ts`（Top-5 召回 29/30 = 96.7%）
- 回归看板 `perf/trend-report.mjs` + 脚本 `perf:memory-recall` — `fe94975` / `cc188ed` / `0eeb4ce`
- 阻塞项标注 README + 实施计划同步 — `67e90bb` / `28db6cd`

**2 项已知阻塞（已记入 `README.md` 末尾「🚧 已知阻塞 / 待办（B-4 记忆召回收尾）」）：**

1. **真实验收 `perf/results/perf-history.json` 为空** —— PRD 性能验收（冷启动 / 内存 / 帧率 / 包体）依赖 `perf/run-all.mjs` 真实产物，需 `pnpm tauri build` + Playwright / 真机 exe；当前 dev 环境拉不起。数据仅本地、不入库。
2. **case 20（运动 → 跑步）语义鸿沟** —— 纯 LCS / 同义词扩展无解，需真实 embedding/RAG 路径（当前评测在 mock 下短路了向量检索），真实 RAG 接通后自然命中。

## 4. 下一步从哪接（未完成项）

**实施计划内（批次三此前误标"待启动"，实际已完成，勿重做）：** A-6/A-7/A-11/B-1 及 A-12~A-15 运行时均已接线（证据见 §2 复核说明）。
真实未做的实施计划项：
- **B-2 自动更新** —— ⚠️ 部分完成：`.github/workflows/release.yml` 已就位（三平台构建 + `tauri signer sign` + 性能门禁）；剩余阻塞为签名私钥（CI Secret）+ `updates.json` 托管，需仓库管理员权限，AI 无法推进。
- **B-3 流式加密 / `.legacy` 清理** —— >1MB blob 落盘加密 + DataPanel 清理 UI（✅ 2026-08-30 复核确认完成：Rust `crypto.rs` ENC3 分块流式加密 + TS `blobCrypto.ts` 分派（>5MB 走 ENC3）+ `visualMemoryManager.ts:193` 已用 `encryptBlob` 落盘 + `DataPanel.tsx` `.legacy` 清理 UI；单测全绿）。
- **C 类代码债清理（C-1~C-7）** —— ✅ 已全部完成（2026-08-30 逐条复核）：
  - C-1 `CharacterCreator.tsx:674` 死 UI 已移除（仅留说明注释，无死代码残留）；
  - C-2 `aiAgent.ts` 权限表已上收 `agentTools.TOOL_MODE_PERMISSIONS`（注释确认）；
  - C-3 `SettingsWindow.tsx` import/order 14 条 warning 已清零（eslint --fix 复检 0 残留）；
  - C-4 `memoryEditor.readMemory` 已返回 `sanitizeMemoryForDisplay(...)` 净化对象；
  - C-5 **人工流程**：Live2D 发行许可申请（见 `docs/LIVE2D_LICENSE_REMINDER.md` checklist），AI 不可代办，发布前由你完成；
  - C-6 `modelHotLoader.ts` 已删除（commit 见原记录）；
  - C-7 `gpuParticleSystem.ts:147` 已用 `typeof document !== 'undefined'` 守卫，Worker 环境安全。

**广义记忆升级计划 `MEMORY_UPGRADE_IMPLEMENTATION_PLAN.md`（P0–P3）：**
- **P0 打分准确性：✅ 已完成（2026-08-29 复核，working tree 未提交）**——三项子任务均已落地：
  - **P0-1** 检索返回真实分：`recallEngine.ts` 记忆候选 `relevance` 由硬编码 `0.7` 改为透传 `checkRelevanceTrigger` 真实检索分（`memoryTypes.ts` 的 `TriggerResult.score` + `retrieve()`/`searchEpisodicWithScores` 返回 `RetrievalResult`）。
  - **P0-2** 非破坏性 consolidate：`applyConsolidation()` 仅巩固 `importance<30` 且 >7 天的旧记忆；源记忆写 `superseded_by` 软删除（非物理 DELETE），并写入结构化语义事实表（`source_memory_ids` 可溯源）。**新增回归测试 `enhancedMemory-consolidation-p0.test.ts`（3 用例：软移除可追溯 / 阈值排除 / 行级 superseded_by + 语义事实溯源）。**
  - **P0-3** RRF 归一化：移除 `Math.min(1, r.score * 61)` hack，改用标准 `1/(rrfK + rank)`（`enhancedMemory.ts` 中 `normalizedRRF`）。
  - 另修复 `enhancedMemory.ts` 一处预存 `tsc` 错误（`Boolean()` 包裹），基线恢复 0 错误。
  - ⚠️ 注意（已修复）：`src/lib/__tests__/enhancedMemory-timeline.test.ts` 曾有一例预先存在的失败（本地时区午夜边界下 `dateKey` 用本地日期、旧测试用 UTC `toISOString().slice(0,10)` 比对不一致），已于本回合改为固定时刻 + 本地日期计算，消除该 flake。
- 仍缺 P1 实体图 PPR 多跳联想 / ownerFacts 双时间轴 / 语义结构化、P2 离线做梦巩固 / 记忆自编辑工具、P3-9 episode 多模态记忆。

## 5. 新会话必须遵守的约束（铁律 / 协议）

- **分支策略**：日常只在 `main`（本地）提交；对外发布合并到 `public` 后 `push origin public`。
  **绝对禁止 `git push origin main`**（会泄漏本地私有代码到 GitHub）。
- **不碰自我进化文档**：`docs/project/KNOWN_GOTCHAS.md`、`docs/project/REVISION_LOG.md`
  由专门流程维护，普通会话不要编辑（即便实施计划第 236 行要求同步，也归该流程负责）。
- **禁止假实现 / 诚实降级**：做不到的部分显式降级为文档化设计稿或纯 CSS 兜底，
  绝不能写占位 TODO 假装实现（AGENTS.md 铁律 #6）。
- **perf 数据本地不入库**：`perf/results/*.json` 与 `*.html` 是 `pnpm perf:trend` /
  `perf:memory-recall` 的再生产物，已 gitignore；趋势源 `recall-history.json` 等也不入库。
- **评测边界要知道**：记忆召回评测在 mock 下短路了向量检索 / RAG，纯本地无法补 case 20；
  真实 RAG 接通后该 case 自然命中。
- **真机冒烟项需人工**：`docs/plans/真机冒烟checklist-20260828.md` 与
  `V1-V4-真机验收checklist.md` 中标注「人工验收」的项 AI 无法替代，发版前逐项勾选。

## 6. 验证命令（改一处即可全量自证）

```bash
pnpm test                 # 全量 vitest（当前 2302 passed / 0 failed）
pnpm lint                 # tsc -b + eslint（0 错误为基线）
pnpm perf:memory-recall   # 记忆 P95 基准 + 30 条召回评测 + 刷新趋势看板
pnpm tauri dev            # 开发运行
pnpm tauri build          # 产出 exe（真实验收前置）
```

## 7. 关键文件索引

- 实施计划：`docs/plans/SpiritPal-下一步实施计划.md`（任务全量梳理 + 批次拆解，已同步 B-4）
- 广义升级方案：`docs/plans/MEMORY_UPGRADE_IMPLEMENTATION_PLAN.md`（P0–P3，多未做）
- 记忆核心：`src/lib/enhancedMemory.ts`（检索主入口）、`recallEngine.ts`、`entityLinking.ts`
- 评测 / 基准：`src/lib/__tests__/memoryRecall.{bench,eval}.test.ts`、`perf/trend-report.mjs`、`perf/*.mjs`
- 协议 / 架构：`AGENTS.md`、`README.md`、`docs/project/PRD_桌面宠物应用_v0.1.md`
- 阻塞项记录：`README.md` 末尾「🚧 已知阻塞 / 待办（B-4 记忆召回收尾）」

---

*本简报是 2026-08-29 的仓库快照（同日已复核更新：批次三与 A-12~A-15 标注为已完成、C-2/C-6 标记完成、P0（P0-1/2/3）已完成（working tree 未提交，含 P0-2 回归测试））。若距生成日较久，先 `git log --oneline -15` 与 `git status` 核对最新状态，再对照 §2/§4 判断进度。*
