# SpiritPal 剩余任务交接报告

> 生成日期：2026-08-27
> 核对方法：以磁盘实际代码为准（`src/`、`src-tauri/`、`scripts/`），对每个功能模块做 import/符号级引用核查，判定其状态为：**已接线**（被运行链路引用）/ **孤岛**（模块已建但无任何 importer）/ **缺失**（无任何代码）。
> 用途：本报告交给执行方 AI 逐项完成。每项均附「落点文件、实施内容、验收标准」。
> 关联文档：`docs/plans/SpiritPal-未实现与待办清单.md`（原始清单）、`学习成果落地分析报告.md`（73 份学习报告的落地分析）

---

## 0. 必读背景（执行前须知）

1. **本项目是 Tauri 应用，webview 不是 Node 环境**。凡是静态 import 了 `node:child_process`、`node:stream`、Node `events` 或含这些依赖的包（如 `@modelcontextprotocol/sdk/client/stdio.js`）的模块，一旦进入前端 bundle 就会构建失败。`src/lib/mcpClient.ts` 因此**从未被任何前端代码引用**——接线时只允许在设置面板等处使用"纯 fetch 实现的 SSE 探测"，或经 Rust 命令桥。
2. **本会话新增了大量"孤岛模块"**：功能代码已写好并有类型/测试保障，但没有接进 UI/状态机/渲染管线。对用户而言这些功能不可见。**剩余任务的主体就是下表 A 的接线工作**。
3. **已存在功能重叠的旧实现**（如 `i18n.ts` vs `i18nManager.ts`、`scheduleManager.ts` vs `calendarIntegration.ts`）。接线前必须先做"合并 or 替代"决策，**严禁双轨并存**。
4. 项目提交规范：按模块分批 conventional commits，附任务编号。
5. 全局验证命令（每完成一项至少过前三条）：
   ```bash
   npx tsc -b
   npx vitest run src/lib/__tests__ tests 2>&1 | tail -5
   npm run build
   cd src-tauri/mcp-server && cargo test   # 涉及 MCP 时
   cd src-tauri && cargo check --lib       # 涉及 Rust 命令时
   ```

### 当前已确认完成且已接线的项（无需重做）

| 项 | 证据 |
|----|------|
| P0-3 MCP Server 全链路（Rust stdio + 命令桥 + Token 鉴权 + webview 执行端） | `src-tauri/mcp-server/src/main.rs`、`src-tauri/src/mcp_bridge.rs`、`src/lib/mcpAppBridge.ts`（main.tsx 已接线） |
| P0-4 memoryEditor 暴露为 MCP 工具（`spiritpal_memory_edit`，真实持久化） | commit `67b498b`：mcpBridge/mcpServer/Rust 三链路对齐，vitest 33/33 |
| P1-MCP 设置页 MCP 管理面板（工具授权 + 外部服务器注册 + SSE 探测） | commit `67b498b`：`McpSettingsPanel.tsx` 挂入 SettingsWindow 'mcp' Tab |
| P1-1 窗口标题提取 | `windowTitleExtractor.ts` ← `contextAwareness.ts`（已接线） |
| usePetDragging eslint 告警 | commit `41f661a` |
| 收藏系统 / TTS / GDPR 删除 / PII 掩码 / 推送通知 / autoMapper / 情感回写 / 视觉记忆召回 / 行级化 / 多分支对话 | 原清单已标 [x] 并经符号级核实（见清单 P0-2、P1 段） |

---

## A. 孤岛模块接线任务（主体工作，按优先级排序）

> 判定依据：`Get-ChildItem src -Recurse | Select-String "import.*<module>"` 无任何 importer。
> 通用验收：接线后该模块必须被至少一个运行链路文件 import 且功能在 UI 可达；`tsc`/`vitest`/`build` 全绿。

### A-1【P0 体验回归】视觉感知"看看"接入交互入口 — `src/lib/visionPerception.ts`

- **现状**：模块完整（截屏 + Vision LLM 分析 + 工作状态推断），无人调用。
- **实施内容**：
  1. 在 `src/components/PetWindow.tsx`（或宠物右键菜单组件）添加"看看"菜单项：调用 `visionPerception.captureAndAnalyze()` → 结果经气泡组件展示；
  2. 分析结果经 `usePetStore` 写入情绪/上下文（与 `contextAwareness` 已有链路复用，避免重复的状态写入路径）；
  3. 静默失败兜底：无 LLM 配置/截屏权限拒绝时展示引导文案而非报错。
- **验收标准**：运行 app，右键宠物 → "看看" → 气泡显示对当前屏幕内容的自然语言描述；`vitest` 为新增调用路径补 1+ 用例。

### A-2【P0 体验回归】隐藏互动状态接入 — `src/lib/hiddenStateManager.ts`

- **现状**：爬墙/探头/躲藏三状态管理器已建（含 PetState 扩展 commit `984ae3c`），但 `PetWindow` 状态机未消费。
- **实施内容**：
  1. 在宠物拖拽释放时（`usePetDragging.ts` 的 `dockDir` 输出）判定贴边 → 调用 hiddenStateManager 进入 climbing/peeking；
  2. 渲染层按状态切换动画/朝向（与 A-4 吸附反馈合并实施最佳）；
  3. 定时退出：躲藏 N 分钟后探头回流（hiddenStateManager 内置计时，UI 层只做订阅）。
- **验收标准**：拖宠物到屏幕左/右边缘松手 → 宠物呈现贴边攀爬或探头姿态；拖回中央恢复正常。

### A-3【P0 体验回归】吸附视觉反馈 Hook 接入 — `src/hooks/pet/useDockVisualFeedback.ts`

- **现状**：Hook 完整（贴边旋转朝向 + 表情变化），`PetWindow` 未调用。
- **实施内容**：在 `PetWindow` 将 `usePetDragging` 返回的 `dockDir` 传入本 Hook，输出的旋转角/表情应用到 Live2D 模型变换（`setModelScale/rotation` 或参数映射）。与 A-2 使用同一 dock 事件源，合并为一个 PR。
- **验收标准**：吸附瞬间可见旋转/表情过渡（非瞬移）；un-dock 后恢复。

### A-4【P0 记忆可视化】挂载 MemoryVisualizer — `src/components/MemoryVisualizer.tsx`

- **现状**：四 Tab 面板（时间线/搜索/情绪分布/知识图谱）组件已建，未进入任何窗口。
- **实施内容**：在 `SettingsWindow.tsx` 的 `memory` Tab 中挂入（现有 `MemoryPanel` 顶部加视图切换："精简 | 可视化"，默认精简，保持旧行为不回归）。
- **验收标准**：设置 → 记忆 → 可视化视图渲染出时间线与情绪分布（有记忆数据时非空）；无数据时空态文案。

### A-5【静默模式生效】接入 silentModeManager — `src/lib/silentModeManager.ts`

- **现状**：管理器完整（手动/计划/会议/专注/临时 + 持久化），无任何消费方——即"静音开关"目前点了不会真的静音。
- **实施内容**：
  1. `ChatWindow.tsx`/`proactiveSpeak.ts`（主动说话链路）发送前检查 `isMuted()`，静默时仅记录待说话队列或丢弃；
  2. 宠物右键菜单 + 系统托盘菜单加"🔕 静默 5 分钟"；
  3. 与 `contextAwareness` 的 meeting 状态联动（进入会议自动静音，`silentModeManager` 已有 reason 机制）。
- **验收标准**：开启静默后宠物在对话/主动场景中不再说话；到期自动恢复；托盘图标有静音角标（可选）。

### A-6【性能管线】帧缓存与精灵图工具接入 — `src/lib/frameCache.ts`、`src/lib/spriteAtlasBuilder.ts`

- **现状**：LRU 帧缓存与 BinPacker 图集构建器已建，Pixi/Live2D 渲染层未使用。
- **实施内容**：
  1. `Live2DRenderer.tsx`（或 pixi 帧动画路径）在帧纹理获取处套 `FrameAnimationCache`，命中率经现有 `runtimeMonitor` 上报；
  2. `spriteAtlasBuilder` 归 `GifToSpriteTool`/`SpriteSheetPanel` 的导出链路（多图合并为图集，减少纹理切换），作为导出选项而非默认行为。
- **验收标准**：连续动画播放时缓存命中率 > 0（devtools 打印）；图集导出产出 PNG+JSON 且游戏内可加载。

### A-7【渲染重构】GPU 粒子 + 批渲染 + Worker 链路 — `src/lib/gpuParticleSystem.ts`、`batchRenderer.ts`、`webglWorker.ts`、`webgl.worker.ts`

- **现状**：四个文件互相引用但**整链在 app 层是断头路**；且 `webgl.worker.ts` 存在硬问题：Worker 环境没有 `document`（`createDefaultTexture` 用了 `document.createElement`）、`requestAnimationFrame` 在 Dedicated Worker 中不可用（Worker 内需用 `setInterval` 或 OffscreenCanvas 的 `requestAnimationFrame`（实验性））。
- **实施内容**：
  1. 修复 worker 环境兼容：纹理生成改为 `OffscreenCanvas.getContext('2d')` + 帧循环改 `setTimeout`/`MessagePort` 驱动；
  2. 决定落点：**建议仅先接"粒子特效层"**（宠物互动彩蛋：抚摸飘心形粒子），经主线程 `<canvas>` 直接跑 `GPUParticleSystem`，暂不启用 Worker 变体（把 `webglWorker/webgl.worker` 标记为实验性并从默认链路排除，加 Vite `build.target` 验证不入主 bundle）；
  3. `batchRenderer` 的 `uploadToGPU` 当前是注释占位（TODO 示例代码）——要么补全真实实现，要么降级为文档化的"设计稿"，**不允许静默假实现**。
- **验收标准**：抚摸宠物时出现粒子且 60fps 不掉帧（perf 采样）；或明确记录"批渲染未接线"避免误导。

### A-8【日记/日程 UI 化】dailyJournal + calendarIntegration + timezoneSync

- **现状**：三个模块全部孤岛。且注意：**项目已有 `scheduleManager.ts`（中文语义解析 + 提醒，已接线）**，`calendarIntegration.ts` 与它职责重叠；`calendarIntegration.ts` 的 `fetchEvents` 是占位空数组（无真实系统日历读取）；`timezoneSync.ts` 的时区转换实现简化（自述"需要 date-fns-tz"）。
- **实施内容**：
  1. **先做冲突决策**（推荐：calendarIntegration 作为 scheduleManager 的"外部日历数据源"插件而非平行系统，两者共用一个提醒调度器）；
  2. 日记：设置页新增"日记"Tab（JSON 文件读写 + Markdown 导出），入口 `dailyJournal.ts.generateTodayDraft`，"生成今日日记"按钮走现有 LLM 链路（llmClient）；
  3. 系统日历读取在 Tauri 下需新增 Rust 命令（Windows: `windows` crate 的 DateTime/Calendar 或调用 `Get-WinEvent`/Outlook API；macOS: AppleScript `calendar`）——若评估成本过高，可降级为 **ICS 文件导入** 并诚实更新模块文档；
  4. timezoneSync：与现有 `i18n.ts`/date 工具合并，不保留平行实现。
- **验收标准**：设置页可生成/查看/导出当日日记（含 LLM 摘要）；日程数据有真实来源（外部日历或 ICS），不再返回空占位。

### A-9【i18n 统一】i18nManager / i18nTranslations / emojiCultureData 三选一决策

- **现状**：**高风险重复**。应用已有 `src/lib/i18n.ts`（支持 zh/en/ja/ko/zh-TW 的字典系统，全应用在用）；新建的 `i18nManager.ts`（Intl API + 独立字典）无人使用。两套并存会造成维护分裂。
- **实施内容**（推荐方案 B）：
  - 方案 A：删除 i18nManager/i18nTranslations，仅保留 `emojiCultureData.ts` 并接给 `proactiveSpeak`/表情选择器（按 locale 选表情风格）；
  - **方案 B（推荐）**：把 `i18nManager` 的增量能力（Intl 日期/相对时间格式化、`{n}` 插值、RTL 探测）**并入现有 `i18n.ts`**，删除平行字典，翻译键按 `AGENTS.md` 规范 `模块名.语义名` 收敛；
  3. `emojiCultureData` 接入聊天/气泡表情选择链路（宠物回复表情按用户 locale 文化适配）。
- **验收标准**：全局只有一个 i18n 模块被 import；切换语言后新增文案（日记/日程/MCP 面板均应为新纳入字典的 key）随语言切换；无硬编码中文残留于已国际化组件。

### A-10【AI 表情引擎】emotionEngine 接入 — `src/lib/emotionEngine.ts`

- **现状**：9+7 情绪分类器已建，但 `ChatWindow` 现用的情感回写走的是旧的 `emotionTagsToMood`（已接线，勿破坏）。
- **实施内容**：将 `emotionEngine` 作为 `emotionTagsToMood` 的**升级替换**（同一调用点），输出驱动：① Live2D 表情参数（经 animationConfig 表情映射）；② MemoryPanel 情绪分布图（与 A-4 共享数据）；③ 每日情绪曲线（喂给 A-8 日记摘要）。
- **验收标准**：输入"今天被老板骂了，好烦" → 宠物表情变低沉 + 记忆情绪值落库为负价，一条链路贯通。

### A-11【记忆摘要/推荐接线】memorySummarizer + memoryRecommendation

- **现状**：P1-3/P1-4 产物（压缩摘要、个性化推荐）完整但孤岛。注意 `memoryBackground.ts` 已接线 entityGraph/dreaming——新模块应挂到同一后台链路。
- **实施内容**：
  1. `memorySummarizer`：接入 `memoryBackground` 的夜间维护任务尾部（生成周/月摘要缓存），`MemoryVisualizer` 时间线 Tab 读缓存渲染"周记"；
  2. `memoryRecommendation`：接入 `proactiveSpeak.ts` 主动说话候选生成（推荐话题优先级 > 随机触发），受 A-5 静默开关约束。
- **验收标准**：一周使用后主动发言话题命中用户高频主题（人工抽检 5 例）；摘要缓存生成日志出现在后台任务。

### A-12【记忆导出/批量操作接入 UI】memoryExporter + batchOperationManager

- **现状**：导出（JSON/CSV/Markdown）与批量操作（多选+事务+撤销）已建，`MemoryPanel`/`DataPanel` 未接入。
- **实施内容**：
  1. `MemoryPanel` 加"导出"按钮（格式选择 + 时间范围）→ `memoryExporter`，**默认沿用现有 PII 掩码策略**（`exportAll` 已接 piiMasking，务必复用而非另起输出通道）；
  2. 记忆列表加多选模式 → `batchOperationManager` 批量删除（二次确认弹窗）；
  3. 若与现有 DataPanel 导出重叠，保留 DataPanel 为总入口，MemoryPanel 只做"仅导出记忆"快捷方式。
- **验收标准**：导出文件字段齐全且邮箱/手机号已打码；批量删除 10 条后列表与 SQLite 行数一致。

### A-13【Live2D 物理】live2dPhysicsParser 接入模型加载 — `src/lib/live2dPhysicsParser.ts`

- **现状**：physics3.json 解析 + 摆/弹簧模拟器已建；Cubism 原生 SDK 本身处理大部分物理，**本模块的适用场景是"非 Cubism 精灵图宠物的伪物理"**（头发/尾巴摆动）。
- **实施内容**：在 pixi 精灵图渲染路径（`PetSpriteRenderer`/相关 hook）中，对含 `physics3.json` 的自定义角色模型，用本解析器驱动装饰部件摆动；不改动 Live2D 模型的既有 Cubism 物理。
- **验收标准**：导入含物理配置的社区角色 → 饰品/发丝随移动摆动；默认 Doro 角色无回归。

### A-14【Mini Mode 补齐】Rust 侧命令 + 构建产物 — `src/lib/miniModeManager.ts`、`src/mini-mode.html`

- **现状（硬缺口）**：前端 invoke 了 `switch_mini_mode`/`set_window_opacity`/`set_window_decorations`/`resize_window`/`mcp_*` 等命令，但 **`src-tauri/src/` 中不存在这些命令实现**（grep 为空）；`mini-mode.html` 未进入 Vite 多页构建输入，产物中不存在。
- **实施内容**：
  1. `src-tauri/src/lib.rs` 新增命令：`switch_mini_mode`（保存/恢复窗口几何 + 显隐第二个 WebviewWindow）、`set_window_opacity`、`resize_window` 等，注册进 `generate_handler!`；
  2. Vite 配置把 `mini-mode.html` 加入 `rollupOptions.input` 多页入口，`tauri.conf.json` 指向可解析路径；
  3. 前端 `invoke` 全部加错误兜底（命令不存在时降级为纯 CSS 迷你态）；
  4. 快捷键注册改用现有 `global-shortcut` 插件链路（silentModeManager 中 F9 预留接线）。
- **验收标准**：托盘菜单"迷你模式"→ 窗口变为 100×100 置顶小窗，双击恢复；`cargo check --lib` 通过。

### A-15【Mod 打包补全】scripts/modPackager.ts 的 ZIP 真实实现

- **现状**：manifest 生成/校验完整，但 `scripts/modPackager.ts:265` 有 `TODO: 使用 archiver 或 jszip 库`——**实际不落盘 .petmod 压缩包**。
- **实施内容**：引入 `jszip`（devDependency，Node 脚本可用）实现真实压缩；`npm run pack:mod` 脚本注册；产物用现有 Mod 导入器回读验证。
- **验收标准**：打包一个示例 Mod 目录 → 产出可解压的 `.petmod` → 应用内导入成功且 manifest 校验通过。

---

## B. 原清单遗留项（非孤岛类）

### B-1【P1-MCP 收尾】mcpHooks 接线 — `src/lib/mcpHooks.ts`

- **现状**：钩子管理器（pre/post + reaction mapping）只有自引用。
- **实施内容**：在 `mcpAppBridge.ts` 的工具执行前后插入 `getMcpHooksManager().runPre/runPost`；`mcpServer.ts` 的 HTTP 模式启动时 `LeaseManager` 已有（保留）。默认钩子：post-hook 把工具调用映射为宠物反应（复用 OPENPETS_REACTION_MAP）。
- **验收标准**：外部 Agent 调 `spiritpal_say` → post hook 触发一次"说话→开心表情"反应链；hook 抛错不阻断工具本身。

### B-2【P2 发布】自动更新发布落地

- **现状**：`updater.ts` + `UpdateNotification` 前端链路已接线；缺外部三件套：GitHub Release 工作流、更新签名私钥、`updates.json` 托管。
- **实施内容**（需仓库管理员权限，AI 可产出脚本与 CI 配置）：
  1. `.github/workflows/release.yml`：构建三平台安装包 + `tauri signer sign` + 上传 Release；
  2. 生成 `TAURI_SIGNING_PRIVATE_KEY`（本地保存公钥到 tauri.conf，私钥存 CI Secret）；
  3. `updates.json` endpoint 写入配置；
  4. 发布前灰度：`gradualRollout.ts` 已存在，接入更新检查。
- **验收标准**：测试 channel 发一个 0.0.1→0.0.2 演练版本，应用内完成下载-验签-安装-重启全流程。

### B-3【P2 安全】at-rest 流式加密 / .legacy 数据清理入口

- **现状**：`encryptedStorage.ts` 存在；行级化后仅旧 blob 回退分支用 `encrypt_data`；`.legacy` 文件无清理入口。
- **实施内容**：
  1. 大 blob（相册/导出文件）写盘前流式分块加密（阈值 >1MB）；
  2. DataPanel 加"清理遗留数据"入口：扫描 `.legacy` → 列清单 → 二次确认删除；
  3. MCP 桥端口安全评审文档（127.0.0.1 绑定 + Token 已有，补充：端口冲突、跨 origin 请求防护结论）。
- **验收标准**：加密文件无法被文本编辑器直读且可正常解密加载；清理入口有 dry-run 列表。

### B-4【P2 质量】记忆评测集 / 性能基准 / V1-V4 真机验收

- **实施内容**：
  1. 用 `perf/` 目录现有脚本模式，补：启动时延、拖拽帧率、记忆检索 P95 三个基准；
  2. 制定 V1-V4 人工验收 checklist 文档（运行 app 逐项勾选，AI 无法替代，需真人执行）；
  3. 记忆召回评测集：30 条构造对话 + 期望召回断言，接入 `vitest`（LLM 部分 mock）。
- **验收标准**：基准脚本可一键产出 JSON 报告；评测集 CI 通过率纳入测试命令。

---

## C. 已知代码债（小而明确）

| # | 位置 | 事项 | 处理建议 |
|---|------|------|---------|
| C-1 | `src/components/CharacterCreator.tsx:674` | TODO：自定义动画行编辑"将在 P1-3 导入向导统一实现" | 与 `characterCardImporter.ts` 合并实现或移除死 UI |
| C-2 | `src/lib/aiAgent.ts:355-368` | Developer/Worker 级工具注册 TODO | 按需注册（文件/终端工具），或降档声明能力边界 |
| C-3 | `src/components/SettingsWindow.tsx` | 14 条 import/order 存量 warning | 单独一次 `eslint --fix` 提交（注意本会话曾发现 --fix 重排导入引发评估顺序变化的风险，fix 后必须全量 vitest 回归） |
| C-4 | `src/lib/memoryEditor.ts` `readMemory` | 返回对象含全部内部字段（MCP 链路已裁剪，但面板直用会泄露内部结构） | 增加 `sanitizeForDisplay` 层 |
| C-5 | `docs/LIVE2D_LICENSE_REMINDER.md` | 发行许可申请是发布阻塞项（人工流程） | 发布前必须完成，AI 不可代办 |
| C-6 | `src/lib/modelHotLoader.ts` | 已标弃用的死桩文件 | 下个 major 前物理删除（先确认无动态 import） |
| C-7 | `src/lib/webgl.worker.ts` | 见 A-7 的 Worker 环境兼容问题 | 与 A-7 一并处理 |

---

## D. 建议执行顺序（4 个 PR 批次）

1. **批次一（用户可感知，最高优先）**：A-1 看看 → A-2+A-3 贴边互动 → A-4 记忆可视化 → A-5 静默生效
2. **批次二（决策与统一）**：A-9 i18n 统一 → A-8 日记/日程决策与接线 → A-10 情绪引擎替换
3. **批次三（记忆纵深）**：A-11 摘要/推荐 → A-12 导出/批量 → B-1 mcpHooks
4. **批次四（基建与发布）**：A-6/A-7 性能管线（含诚实降级）→ A-13 物理 → A-14 MiniMode Rust 命令 → A-15 Mod 打包 → B-2/B-3/B-4 → C 类清理

> 每批次完成标准 = 该批任务验收全过 + 全局验证命令全绿 + 按模块 conventional commit（记录任务编号，如 `feat(mcp): 接线 mcpHooks 至命令桥 (B-1)`）。

---

## E. 孤岛模块清单速查（当前实测，供接线后逐一销项）

```
visionPerception  hiddenStateManager  useDockVisualFeedback  MemoryVisualizer
emotionEngine     frameCache          spriteAtlasBuilder      silentModeManager
memoryExporter    batchOperationManager  miniModeManager(+mini-mode.html)
dailyJournal      calendarIntegration    timezoneSync
i18nManager(+i18nTranslations+emojiCultureData)
memorySummarizer  memoryRecommendation
gpuParticleSystem→batchRenderer→webglWorker→webgl.worker（链内互引，链头断）
live2dPhysicsParser
```
已接线勿重复处理：`windowTitleExtractor`、`mcpPermissions`、`memoryEditor`（MCP 链路）、`mcpAppBridge`、`scheduleManager`、`i18n.ts`、`entityGraph/dreamingConsolidation（memoryBackground）`。

*本报告全部结论来自 2026-08-27 磁盘状态实测（import 级核查），与任何历史汇报口径无关。*
