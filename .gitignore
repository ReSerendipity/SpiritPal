# Dependencies
node_modules/
.pnpm-store/

# Build outputs
dist/
dist-ssr/
build/
out/
target/
*.local
*.exe
*.dll
*.so
*.dylib

# Tauri
src-tauri/target/
src-tauri/WixTools/
src-tauri/gen/schemas/
src-tauri/gen/ios/
# Android build artifacts (project config files are tracked)
src-tauri/gen/android/.gradle/
src-tauri/gen/android/build/
src-tauri/gen/android/.idea/
src-tauri/gen/android/local.properties
src-tauri/gen/android/keystore.properties
src-tauri/gen/android/app/build/
src-tauri/gen/android/app/.cxx/
src-tauri/gen/android/app/.externalNativeBuild/
src-tauri/gen/android/app/src/main/jniLibs/
src-tauri/gen/android/app/src/main/assets/

# Mobile / package artifacts
*.apk
*.aab
*.ipa
*.app/

# Env / secrets
.env
.env.local
.env.*.local
*.pem
*.key

# IDE / OS
.vscode/*
!.vscode/extensions.json
.DS_Store
Thumbs.db
*.swp
*.swo
*~

# Logs
logs/
*.log
npm-debug.log*
pnpm-debug.log*
yarn-debug.log*
yarn-error.log*
debug.log

# Testing
coverage/
.nyc_output/
test-results/
playwright-report/
blob-report/
playwright/.cache/

# Cache / temp
.cache/
.turbo/
*.tsbuildinfo
*.tmp
*.bak
fix-imports.js
update-imports.ps1

# Reference repos (vendored references, not part of project source)
references/

# Build artifacts
artifacts/

# Local tooling state
.trae/
.qoder/
.idea/

# IDE canvas files
*.canvas.tsx
*.canvas.status.json

# Android signing & local SDK
android-sdk/
*.jks
*.keystore

# R-05: Tauri Updater private key (DO NOT COMMIT)
src-tauri/keys/*.key
src-tauri/keys/*.key.pub


# 疑似 API 密钥轮换记录（sk-...==>sk-...），严禁入库；确认无效后删除该文件即可
expressions.txt
docs/repo_research/
# 归档用：第三方仓库克隆（含各自 .git），不入库
_devarchive/repo_research/


# Python
__pycache__/
*.py[cod]

# AI 规范（仅本地保留，不上传远程，2026-08-27）
AGENTS.md
LOCAL_RULES.md

# 跨项目治理文档（整理/核对报告，仅本地保留，2026-09-02）
docs/六大项目文档整理与核对.md
# 治理/分析文档（仅本地保留，不上远程）
docs/repo-analysis/

# 内部开发文档（仅本地保留，不上远程，2026-09-02）
_devarchive/
docs/FILEMAP.md
docs/README.md
docs/_devarchive/
docs/plans/
docs/project/*
!docs/project/KNOWN_GOTCHAS.md
!docs/project/AI_DEV_SOPS.md
docs/reports/
docs/analysis/
docs/整理记录_20260823.md

# 本地预检脚本（不入库）
precheck.ps1

# CI 修复交接日志（本地）
FIX_LOG.md

# 性能趋势看板构建产物（由 pnpm perf:trend 重新生成，非源码）
perf/results/perf-trend-dashboard.html
perf/results/perf-trend.json

# 记忆召回趋势数据（每次 pnpm perf:memory-recall 刷新，本地可再生，不入库）
# 与既有 perf-history.json 约定一致：趋势数据仅本地，源码 trend-report.mjs 入库
perf/results/memory-recall-p95.json
perf/results/memory-recall-accuracy.json
perf/results/recall-history.json

# 性能门禁运行时产物（单次测试结果，机器相关，不入库）
# 基线基准 perf/results/baseline.json 例外 — 需提交入库作为长期回归基准（P1-02/03）
perf/results/cold-start.json
perf/results/memory-usage.json
perf/results/fps-test.json
perf/results/model-switch.json
perf/results/model-switch-gui.json
perf/results/drag-fps.json
perf/results/memory-leak.json
perf/results/package-size.json
perf/results/perf-run-latest.json
perf/results/perf-history.json
perf/results/long-session-results/
perf/results/penetration-test.json
docs/agents/

# --- 2026-09-06 目录整理补齐（方案 A：路径不动，仅排除）---
docs/_devarchive/repo_research/
.mypy_cache/
pytest_tmp/
.hypothesis/
.workbuddy/
.workbuddy-ai/
.zcode/
_tmp_excluded_tests/

# --- 社区宠物接入（2026-09-11）---
# 第三方仓库本地克隆（WindowPet 素材源，含版权二创素材，不入库）
scripts/vendor/
# 本地工具链（ffmpeg 便携版）
scripts/tools/
# 转换中间产物
.tmp-*/

# ── 2026-09-12 本地噪音排除（按仓库主人裁定）──
# 本地 git 目录备份（不入库）
backups/
# 运行时日志（app_log.txt 等 .txt 日志；*.log 已覆盖 .log 后缀）
app_log.txt
*.log.txt
# 误落入仓库根的 Windows AppData 副本（外部 agent 工作区，非项目内容，仅忽略不删）
AppData/
# 本地便携启动脚本（仅本机）
start-portable.bat
# 宠物资源（宠物文件，先不入库，待定）
public/pets/
